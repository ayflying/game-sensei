# Game Sensei 开发与协作规范

> 本文是 AI 和开发者执行本项目的主约束文档。任何开发、修复、实验、训练或发布工作，必须先阅读本文，再阅读 README 的对应章节。若代码现状与本文冲突，以“当前基线”中已验证的事实为准，并在同一变更中更新本文。

## 0. AI 执行总规则

1. **先理解，后修改**：先检查工作区、相关代码、测试和最近提交；不得凭文件名猜测实现。
2. **小步变更**：一次只解决一个明确目标，不顺手增加未请求功能、不重构无关模块。
3. **不以代码存在代替功能完成**：必须区分“已实现、已单测、已本地验证、已真机验证、已发布”。
4. **不覆盖用户工作**：发现未提交修改时，只能修改明确相关文件；不得 reset、checkout、clean 或删除未完成探测产物、训练样本及必要证据。已完成任务的临时截图按 AGENTS.md §0.5 清理；新增样本、证据、临时文件必须按用途集中存放，禁止散落项目根目录。
5. **先安全后智能**：所有动作必须经过前台/目标存活检查；目标丢失、设备断连、窗口变化时立即停止，不得继续向未知目标发输入。
6. **失败要可定位**：错误必须包含阶段、动作、原因和安全状态；禁止静默吞错。
7. **完成后闭环**：运行格式化、单测、构建和必要的真实验证；更新文档、状态和已知限制。
8. **常用功能正式实现**：图片理解、配置加载、后端兜底等反复使用的能力必须沉淀到受版本管理的 Go 包及 `cmd/` 命令，先复用既有实现，不反复写临时代码解决。不为此无差别重写训练工具等已有正式 Python 工具；一次性探针不能成为长期默认入口。渠道、模型、密钥从项目根目录 `.env` 读取，禁止硬编码真实环境值。

## 1. 项目目标与硬边界

### 1.1 核心愿景：学会玩游戏，而不是背游戏

本项目的最终目标不是训练一个“只会几款游戏”的模型，而是训练一个能够迁移到大量不同游戏中的通用游戏智能体。智能体应学习可复用的游戏能力、操作规律和决策方法，再通过少量目标游戏信息完成适配。

必须始终区分三层知识：

1. **通用感知层（核心资产，唯一可跨游戏累积的部分）**：视觉理解、界面/场景识别、元件与相对位置检测、可交互性判断、遮挡与可见性、状态读数识别。这一层**不含动作语义**，因此既能跨游戏共用，也能被**无标签数据**（视频帧）持续加厚。
2. **决策/策略层（按游戏可分，不可无条件混训）**：角色控制、方向移动、点击/拖拽/长按、目标追踪、资源管理、战斗反应、探索、避障、任务执行、失败恢复。**这些是「策略」而不是「能力」**：输出维度绑定该游戏的动作语义——同一输出维度在 A 游戏是「向前移动」，在 B 游戏可能对应「点某个按钮」。无条件混训会让梯度互相冲突，并把模型更快推向 mode collapse。**要跨游戏共用一个策略头，必须先引入任务条件输入（task id / game embedding）；没有条件输入时，只能是「每游戏一个头」。**
3. **游戏类型层（可迁移策略）**：RPG、动作、策略、模拟经营、卡牌、平台跳跃、解谜、射击、竞速等类型的共同规则、节奏和决策模式。以文档与 hints 形式承载（见 `outputs/game-sensei-游戏类型知识库.md`），**不直接等同于权重共享**。
4. **游戏适配层（配置与少量学习）**：具体游戏的按钮位置、动作映射、角色/道具名称、目标定义、地图知识和特殊规则。

开发时优先把能力沉淀在第 1 层；第 2、3、4 层只能通过 profile、任务定义、hints 和适配数据表达，不能把具体游戏逻辑硬编码进通用模型或核心模块。新增游戏的成功标准是“低成本适配并复用既有能力”，不是重新训练一套从零开始的专用模型。

构建游戏无关的视觉游戏辅助框架：截图感知 → 通用状态表示 → 通用技能/策略决策 → 平台输入 → 环境反馈 → 轨迹记录 → 教师评估/示范 → 跨游戏训练。

### 1.2 通用化原则

- **能力优先于名称**：模型学习“移动到目标、避开危险、选择可交互对象”，而不是只记“某游戏里的某按钮”。
- **技能组合而非脚本堆积**：用可组合技能（移动、观察、交互、战斗、收集、导航、恢复）解决任务，禁止为每个游戏复制一套流程脚本。
- **相对位置优先于绝对坐标**：屏幕元素应表示为相对位置、形状、颜色、语义和可交互关系；绝对坐标只放在游戏适配 profile。
- **先识别再行动**：先判断画面状态、目标、可执行动作和风险，再输出动作；看不懂时观察或请求教师，不得盲动。
- **跨游戏评测**：任何新能力至少在两个不同游戏或游戏类型上验证，避免把偶然有效当成通用能力。
- **适配成本可量化**：记录新增游戏所需的 profile 字段、示范样本数量、训练时间和首次成功率。
- **策略头不跨游戏混训**：多游戏动作样本汇入同一个策略头属于**未经验证的假设**，不是默认做法。要做必须先有任务条件输入（task id / game embedding），并用「目标游戏独立测试集」证明不退步；没有证据时保持每游戏独立头。数据量不能代替标签语义的一致性：本项目实测弱标签 1419 样本 val_acc 恒 52.36%（=众数类占比），而真机示范仅 60 样本即 66.67%。

### 1.3 硬边界

- 只使用屏幕视觉和模拟输入。
- 不读取游戏内存，不拦截、篡改网络封包，不实现绕过反作弊的方案。
- 学生模型负责实时回路；教师模型只能运行在异步教学回路，不能阻塞实时控制。
- PC 与安卓必须通过统一 backend 和动作协议接入；游戏差异放入 profile，不在核心逻辑内硬编码。
- 联网游戏存在 ToS 和封号风险，默认只用于单机、自研、测试或明确授权环境。

### 1.4 已实测的可迁移性边界（不得当成「已有能力」）

以下三条都是本项目**已跑过对照实验**的结论，写在这里是为了防止把待验证假设当基线：

1. **跨域骨干迁移 = 0，共享骨干不是现成能力。** `video_v1`（视频抽帧域，640×360）在真机帧上直评 **0.00%，且恒输出 `tap`**（README §11.15 配方 A）。⇒ **跨游戏共享视觉骨干是一个待验证假设，不是既有资产**；要用视频数据，必须先解决域对齐（同分辨率、同 UI 布局），再做对照实验。
2. **单帧观测下方向类本质不可分。** `up/down/left/right` 的区别**在帧间、不在帧内**，所有配方（含从零 400 epoch）都没能把 `up` 学起来，方向样本全挤进 `press`/`down`（README §11.15）。⇒ 这是**架构上限，调超参无效**；要学方向必须先换观测（帧差 / 帧对 / 短时序），换观测之前不得把「方向准确率」列为可达成指标。
3. **弱标签会主动污染训练。** 视频判读的「操作」是推断值，判读报告自身声明不可作真值；1419 条弱标签训两版学生 val_acc **恒定 52.36% / 63.49%**（等于各自众数类占比）＝ mode collapse。⇒ **禁止把推断出的动作作为动作真值进训练集**（详见 `outputs/game-sensei-视频训练可行性实测.md`）。

### 1.5 已决策的路线事项（2026-09-13 定档，含复核触发条件）

| 事项 | 决策 | 复核/解冻条件 |
|---|---|---|
| 观测改造（帧差/帧对/短时序） | **不提前**，仍作为进入 P6 的前置条件 | P3 出现「失败归因于帧内信息不足」，或 P6 重采成本 > 提前改造成本 |
| 共享骨干对照实验 | **现在不做**（两款游戏域差过大，负结果无法解释） | 接入第三款且与现有某款同分辨率/同 UI 布局；两游戏各有 ≥100 条干净示范；有可比的独立测试集 |
| 「资料 → 老师」路径 | **先行落地**，见 `outputs/game-sensei-资料到老师落地清单.md` | — |

决策详情与理由见 `outputs/game-sensei-计划评审与方向校正.md` §四。

### 1.6 单游戏线的收尾判据（防止「永远玩不完」）

游戏内容探索是**开放清单**（关卡递进、分支结算、界面入口永无尽头），拿它当收尾标准会让该游戏
**永远无法结束**。收尾判据必须围绕**能力证据**，且可勾选完毕。三条路线各有自己的判据：

| 路线 | 适用 | 收尾判据（全部勾完即完成） |
|---|---|---|
| `-plan` 确定性执行 | 流程固定、操作确定 | ① 端到端真机跑通 ≥1 次（起点/终点状态校验通过，**非 `strict=false` 静默跳过**）② 连续 ≥10 次**打成 WINNER** ≥80%（**胜利 ∧ 终态正确**），且每次失败可归因到具体 step 与判据 ③ 抓帧失败必须重试，**不得当成「判据不满足」** ④ README 补该游戏章节 |
| 学生模型 | 反应式 / 连续决策（pixel→action 写不出规则） | ① 独立测试集显著高于众数基线 ② 达 P0 的连续运行时长与动作类成功率 ③ 有评测报告与回退基线 |
| 老师探索 | 未知界面 | 一次性产出物（hints / plan 候选）交付即结束，**不单列为阶段** |

**Sparkle 当前定位与收尾现状（2026-09-14 跑批后）**：`-plan` 线（纯点击、流程确定，
训学生性价比低于 plan，见 2026-09-13 日志）。**两轮 20 局真机跑批已出，合并胜率 30%（6 胜 14 负）**
（第一轮 run21–30 为 40%、第二轮 run31–40 为 20%——**这个波动本身就是证据**）：

- ① 端到端含终态校验跑通：**✅**（run29 / 35 / 39 胜局 + 独立抓帧确认回主界面）。
- ② 连续 ≥10 次 **WINNER ≥80%**：**❌ 实测 30%**（两轮 40% / 20%）。**根因不在执行**——
  14 局败局的装备步与 Change 步**全部执行过（动作失败 0 次）**，败因是**装扮内容**没选对
  （本作要选到与主题匹配的造型，盘面有 `Level N / 地名`、装扮页有 `Style Tips` 主题），
  属**决策·策略层**——`-plan` 是执行层，只保证「选了什么就执行到位」，保证不了「选对」。
- ③ 抓帧失败重试：**✅**。④ README 章节：**✅（§12.8）**。⑤ 判据能捕获败局：**✅**
  （14 局 LOSE 退出码均为 1，终态帧独立复核为 LOSE 页）。
- 新增两条方法论坑（见 README §12.8）：**跑批统计口径**（按判据串 grep 会漏掉胜局——
  成功行的日志里根本没有判据描述，必须**按步名**匹配）、**胜局后 SALE 弹窗**遮住 ADS 红标
  导致「终态正确」不达标（已加档案末步兜底，第二轮 2/2 胜局已验证生效）。

⇒ **收尾口径修正**：Sparkle 的 `-plan` 线以 **①③④⑤** 收尾；**②改判为「胜负判定正确 ∧
败局可捕获 ∧ 终态正确」**，合并 30% 胜率作为**基线**如实记录、不粉饰。「稳定打赢」需在**决策层**
另做（读 `Level / 主题 / Style Tips` → 选卡），不在本线范围。**下一款 `-plan` 游戏优先选
胜负由操作决定的类型**（节奏 / 反应 / 固定解法），否则同样会卡在判据②上。

**⚠️ 基线更新（2026-09-15，12 步新档远程 10 局）**

旧「20 局 30%」出自 12 步**之前**的档案且**含假 WINNER**，只应作历史记录、**不得再作当前基线**。
新档（12 步 / 19 按钮，终点=结算页）在远程 MuMu 模拟器跑批 **10 局 = 5 胜 5 负（50%）**，
**终态一致性 10/10**，大字判据分离 **≈5.5 倍**（胜 11.6~13.4% vs 负 2.21~2.23%，阈值 6%）；
新档累计（3 局 + 10 局）= **13 局 6 胜 7 负 ≈46%**。⇒ **当前基线 = 胜率 50% · 终态一致性 100%
· 判据分离 ≥5 倍**（方法学与逐轮表见 `docs/plan-sparkle.md` §六）。

- ② 仍**不满足**：50% 远低于 80%，且 10 局内出现 **5 连败**——胜/负在时间上**成块出现**
  （run1–5 全胜、run6–10 全负），**不是随机抖动**。因跑批未记录每局主题，**不进一步归因**，
 只说到「胜负由内容决定」。⇒ 本线**不必再刷局数**：再加局数只会更精确地测出同一个「内容决定」
  的分布，属决策层的事。
- **新增一条方法论（本批暴露，已写入跑批脚本与文档）**：**跑批脚本的终态判据必须与 plan 的终点
  对齐**。plan 终点既然收敛到结算页，就不能再用「终态帧必须回到主界面」校验 —— 那会对**每个
  胜局假报失败**（本批前 9 轮即如此）。正确判据 = **「终态帧上的胜负标记」== plan 判胜步的结论**，
  形成双向独立验证；`.workbuddy/sparkle/remote/check_verdict_consistency.py` 可**离线重算任一批次**，
  用旧口径记的历史批次也能事后纠正，不必重跑设备。

**收尾任务书已出（2026-09-14）**：`outputs/game-sensei-sparkle收尾任务书.md`——给出可勾完的 DoD 与
仅剩的两个缺口（终态校验 / 批量证据）。关键机制结论：**「跑完」≠「成功」**——`Runner.Run()` 只在
单步**报错**时才失败，而 `Step.Strict` 默认 `false`，超时会被「宽松跳过」吞掉；更隐蔽的是末步
`exit_result` 带 `when` 守卫，流程走偏时**整步跳过、终态根本没被校验**，进程照样退出码 0（假成功）。
故终态判定必须由跑批脚本**独立抓帧**完成，且判据要**从档案读取、不得硬编码**（阈值会被调参，
硬编码会静默漂移）。

**⚠️ 判据必须含「胜负」，不能只判「回到主界面」（2026-09-14 实测补充）**

判据②此前写作「成功率」，但**未定义成功是否含胜利**。实测发现现有判据存在**胜负盲区**，
会把一局败局判成 PASS：

- 结算页紫色判据区 `[0.36,0.888,0.64,0.935]`（档案 `change` / `exit_result` 步在用）实测：
  **WINNER 领奖浮层 64.2% · WINNER（点掉浮层后）0.0% · LOSE 93.7%**。
- 档案注释称「阈值 0.6 同时覆盖结算页两态——WINNER 领奖浮层(65.4%)、真 Exit(93.9%)」，
  但该 **93.9% 与实测的 LOSE 页（93.7%）吻合**，而 WINNER 点掉浮层后实测为 **0%**。
  ⇒ 那个「真 Exit」**极可能就是 LOSE 页的紫色 Exit 按钮**，不是 WINNER 的另一种形态。
- 后果链：LOSE 页因紫色 Exit 达标 → `exit_result` 步正常执行 → 正常回到主界面
  → 终态校验（ADS 红标）通过 → **一局败局被记为成功**。而 WINNER 点掉浮层后底部无紫色，
  `exit_result` 步的 `when`（`min_ratio 0.8`）反而不满足而**整步跳过**——**输赢两侧的判定是反的**。

⇒ 判据②必须明确为「**WINNER ∧ 终态正确**」。

胜负判定**可以落进档案、不必外挂脚本**：把「点掉 `No,thanks` 后**等紫色消失**」
（`ratio` 的 `max_ratio: 0.1`）写成一步，配 `strict: true` + `timeout_ms`——
**WINNER 点掉浮层后紫色为 0%，立即满足通过；LOSE 页紫色恒为 93.7%，3s 超时 → `strict` 触发失败**。
这样 plan 自己就会「因输而失败」，退出码非 0，胜率可直接从退出码统计。
而**终态判定仍需跑批脚本独立抓帧**——理由不同：末步带 `when` 守卫时整步会被跳过，
`strict` 也拦不住，属于另一类假成功。

**下一款游戏选型**：先按「下一步动作是否确定」选路线，**不要默认「再训一个模型」**。
若要为 §1.5 的共享骨干对照实验积累有效对照，应优先选**与现有某款同域**（同分辨率 / 同 UI 布局）
的游戏——Sparkle 为安卓竖屏 900×1600，选安卓竖屏类可直接构成对照；换一个类型只会再得一个孤立样本。

**⚠️ 同域新样本 YoYa Star 的实测结论（2026-09-15）：同域 ≠ 可用，先确认「能走完新手教学」**

按上面「优先选同域」原则试了同厂同域的 **YoYa Star: On Air**
（`com.yoyaworld.dressup.makeup.asmr.cosplay.fashion.style`，YoYa World，900×1600 竖屏，
换装 + 直播 + 实时 PK）。冷启动 → 服务条款 → 开场剧情 → 捏人 → 首次直播 → **直播换装界面**
全部走通；但在**新手教学「换衣服」环节硬阻塞**：游戏自动把分类切到「衣服」后**物品网格不刷新**，
教学模态此后**吞掉全部输入**（含 Android 系统返回键），`shot -delta` 量化为 **Δ 1~4**
（有效点击为 20~180），**3/3 次冷启动稳定复现**。

⇒ **同域只保证「界面形态可复用」，不保证「游戏能玩」**。教学阶段的硬阻塞属**不可解阻塞**
（模态吞输入 ⇒ 任何 plan 都点不动），继续投入只会烧预算。
本轮产出是**一份可复现的缺陷记录**（`YoYa Star` 笔记 §五：BUG×3 / ENV×3 / MIS×3 / GAP×2，
已入 ima「游戏」KB），并**暂停该游戏、转回 Sparkle 线**。

⇒ **选型原则补一条**：动手前先确认「**能走完新手教学**」—— 教学是最脆弱的一环，
它卡住则该游戏的一切路线都不成立。诊断顺序与止损准则已沉淀为跨游戏通用方法，
见 `outputs/game-sensei-游戏类型知识库.md` §4.7。

**⚠️ 推翻上述暂停结论（2026-09-15，本机真机 MIX 3 / 1080×2340 复验）**

换到**本机真机**后，**整条新手链一次走通**：模拟器上 3/3 复现的「换装教学硬阻塞」**未复现为硬阻塞**
⇒ 该缺陷至少部分是**模拟器 ENV 叠加**，**BUG-01/02/03 定性降级**，原「等官方修复、暂停该游戏」
的**依据失效**。于是本作重新成为 `-plan` 线的**第二款游戏**（第一款 Sparkle）。

**`-plan` 线已落地并通过真机跑批验收（档案 `internal/game/profiles/yoyastar.json`，14 步）**：

- ① 端到端真机跑通（起点/终点状态校验通过，非宽松跳过）：**✅**（6 轮连跑里含 WIN / FAIL / 未决三类结局，均由**外部独立抓帧**复核）。
- ② 连续 ≥10 次 WINNER ≥80%：**❌ 实测 33~43%**，与 Sparkle 同因——**胜负由内容决定**（要选到与主题匹配的造型），属**决策·策略层**；`-plan` 是执行层，只保证「选了什么就执行到位」。⇒ 同 Sparkle，**以 ①③④⑤ 收尾**，胜率作基线如实记录、不刷局数。
- ③ 抓帧失败重试：**✅**（引擎侧 `grabRetries=3`，本批未出现抓帧失败）。
- ④ README/docs 补该游戏章节：**✅**（`docs/plan-yoyastar.md` §12.11~§12.16）。
- ⑤ 判据能捕获败局：**✅**（b05 / b07 退出码 1，独立复算为 FAIL，败因归到第 12 步「败局闸门」）。
- ⑥（新增）**未决可识别、不误判**：**✅**——b08/b09/b10 三局因插屏广告粘住（返回键清不掉）被诚实记为「未决」（退出码非 0 + 结论 UNDECIDED），**未误判成胜或负**。本作结算前后**必弹第三方广告**，这是环境未决、不是 plan 缺陷。

**⚠️ 本线新增的核心方法论（比多跑 10 局更有价值）**：**「双向一致」不等于「判据正确」**。
4 轮连跑曾给出「plan 自报 == 独立复算」的 **4/4 一致性**，其中一局其实是插屏广告——广告顶部的**黄色块**把「金冠判负判据」顶到 14.5%（真 FAIL 只有 4.4%），**两侧用同一条判据算出同一个错误结论**。
⇒ 铁律：**一致性只防「解析/口径漂移」，判据正确性必须单独与反例帧（广告 / 主界面 / 过渡态）比分离度**；
**否定类结论（判负）必须双证据**（目标特征在场 **AND** 目标页面特征在场）。
附带两条工程纪律：**每轮证据必须落盘**（`end.png` 是唯一物证）、**汇总统计读有 schema 的产物而不是正则猜 stdout**（旧跑批脚本的汇总表因此整列是错的）。
另新增**离线「设计验证矩阵」**（逐步 × 各页面）补齐真机永不触发的守卫分支，并据此发现**结算页有两种版式**（一种不带票数条）。
完整复盘见 `docs/plan-yoyastar.md` §12.13 / §12.16。

## 2. 当前基线与真实结论（2026-09-12）

### 2.1 已完成

- PC 后端：GDI 截图、窗口查询/恢复/置顶/最小化、SendInput 键鼠、右下角日志浮窗。
- 安卓 ADB 后端：截图、设备选择、启动应用、前台检测、tap/swipe/long press/joystick/key。
- 统一动作协议：L1 语义动作经 profile 展开为 L2 平台动作，坐标使用 0~1 归一化。
- Teacher：Ollama/OpenAI 兼容接口、多图送审、content/thinking 双字段兜底、结构化评估、示范轨迹。
- Dataset/Video：抽帧、去重、切段、轨迹和报告落盘。
- Student：纯 Go 前向推理、JSON 权重、训练器和一致性检查。
- 已有游戏档案：`nrc`、`nrc_pc`、`jieyou`、通用 PC/移动档案。

### 2.2 已验证限制

- PC 洛克王国：世界的用户态键鼠注入被反作弊拦截；不得宣称 PC 已实现可用自动操作。
- ADB 截图约 0.94 秒/帧，输入约 0.3~0.6 秒/次；安卓默认 5 FPS，不能套用 PC 的 30 FPS 假设。
- 摇杆必须从有效热区中心起手；地图视野改变会导致固定坐标失效。
- 点任务追踪只触发转视角，不等于持续自动寻路；长距离移动需使用已验证的传送流程。
- Student 仍需要更多真实成功轨迹，Phase 3 自动飞轮尚未完成。

## 3. 架构与职责边界

### 3.1 数据流

```text
Backend.Grab
  -> capture/vision
  -> student/agent
  -> agent.Action(L1)
  -> game.Resolve(profile)
  -> agent.Action(L2)
  -> Backend.Apply
  -> trajectory/memory

teacher（异步）读取帧 + trajectory
  -> 评估/示范
  -> dataset 质量检查
  -> trainer
  -> 候选模型
  -> 离线评测
  -> 人工/规则批准后启用
```

### 3.2 模块职责

| 模块 | 允许负责 | 不允许负责 |
|---|---|---|
| `capture` | 抓屏、裁剪、降采样 | 游戏规则、动作决策 |
| `vision` | 图像编码与基础视觉处理 | 直接发送输入 |
| `agent`/`student` | 状态到动作、推理 | 平台坐标硬编码、训练 |
| `game` | profile 加载、校验、L1→L2 | 设备 I/O |
| `android`/`input` | 平台输入与设备状态 | 教师判断、模型训练 |
| `teacher` | 异步评估、反馈、示范 | 阻塞实时回路、直接越权操作 |
| `dataset`/`memory` | 轨迹、帧、元数据管理 | 自动把未审核数据送入训练 |
| `trainer` | Python 训练、导出、量化 | 运行时控制 |
| `cmd/*` | 编排、参数、生命周期 | 在包内复制业务实现 |

### 3.3 核心接口约束

```go
type backend interface {
    Grab(width int) (image.Image, error)
    Size() (int, int)
    Describe() string
    Live() bool
    Apply(act agent.Action) error
}
```

新增后端必须实现以上能力，并补充：设备断连、前台丢失、截图失败、动作失败测试。

## 4. 动作与 profile 规范

- L1 只表达跨平台意图：`MOVE`、`PRESS`、必要时 `WAIT`/`NONE`。
- L2 表达平台动作：tap、swipe、long press、joystick、key、mouse move。
- 坐标必须归一化；禁止在 teacher prompt、agent 或 backend 中散落绝对像素。
- profile 必须包含：目标名称、平台、分辨率假设、动作映射、按钮热区、前台识别信息、风险说明。
- profile 修改必须提供坐标来源、截图证据或真机验证记录。
- 未知动作不得静默返回成功；必须返回明确错误。`NONE` 才可以安全忽略。

## 5. 开发阶段与可交付物

### P0：安卓真实闭环（当前最高优先级）

**目标**：启动应用 → 前台确认 → 截图 → Student 决策 → 触控 → 结果记录。

任务：固化 `nrc`/目标游戏档案、坐标校准、前台检测；增加动作超时/重试/断连停止；覆盖移动、点击、长按/滑动、返回恢复；采集性能指标。

**验收**：连续真实运行 ≥10 分钟；至少 4 类关键动作有成功率；记录截图/推理/动作/端到端延迟；断连或切后台不会误操作其他应用。

### P1：数据集和教师示范

统一 `meta.json`、`trajectory.jsonl`、帧、教师结论的 run/session/sample ID；加入目标完成、动作合法、帧覆盖、清晰度、重复帧和缺失视角检查；人工复核后才允许进入训练集。

**验收**：样本可追溯、可重建、可拒绝；相同输入重复生成结果一致；不合格数据明确列出原因。

### P2：训练与模型治理

固定数据集版本、配置、随机种子；支持 BC/DAgger；输出 Student 支持的权重；建立离线回放评测、基线、候选、发布、回滚状态。

**验收**：每个模型有评测报告；新模型必须优于基线并满足误操作阈值；失败自动回退；不能因“成功生成权重文件”自动启用。

### P3：自动化飞轮与泛型化

串联采集 → 教师评估 → 选优 → 训练 → 评测 → 候选 → 启用；支持暂停、取消、重试、审计、热重载和异常回退；至少两个游戏通过同一套回归检查。

## 6. 标准开发流程（AI 必须遵守）

### 开始前

1. 查看 `git status`，记录已有未提交文件。
2. 阅读 README 对应章节和本文件相关约束。
3. 搜索入口、调用链、现有测试和配置来源。
4. 写出本次目标、影响文件、验收命令和不做事项。

### 实现中

1. 先补失败路径和安全检查，再接主流程。
2. 优先修改最小文件集合；不改变公开协议除非任务明确要求。
3. 关键规则必须有单测；真实设备行为必须有探针或端到端记录。
4. 日志记录 run ID、目标、动作、结果、耗时；不得记录密钥。

### 完成后

```bash
gofmt -w <本次修改的.go文件>
go test ./...
go vet ./...
go build ./...
git diff --check
git status
```

Windows/安卓功能还必须补充真实环境验证。最终报告必须列出：改动文件、测试结果、真实验证环境、指标、未解决问题、是否可发布。

## 7. 数据、模型和产物状态

```text
raw（原始采集）
  -> reviewed（已复核）
  -> trainable（可训练）
  -> candidate（候选模型）
  -> released（已发布）
```

禁止覆盖稳定模型；每个模型关联数据集版本、训练配置、提交号、时间和评测报告。候选模型未通过评测不得热重载。

## 8. 安全停止规则

出现以下任一情况，立即停止动作并记录原因：backend 不 Live、设备断连、应用不在前台、窗口标题不匹配、截图为空/异常、动作解析失败、动作超时、连续卡死、目标状态未知。恢复前必须重新确认目标，不得自动猜测继续。

**已知可恢复的例外**（必须仍要记录，不得静默）：设备息屏导致的**全黑帧**（见 README §9.5「设备息屏」）。
处理次序是：判定为黑帧 → 发一次 `KEY wakeup` → 等 2 秒重取 → 仍黑则**跳过该步且不落示范样本**、
重置卡死计数；若连续多步仍为黑帧，则按上面的规则停止。息屏是外部环境问题，
把它当成「目标状态未知」直接停机，会让整轮采集因一个可自愈的原因白白中断。

## 9. AI 任务模板

```text
任务目标：
范围：
明确不做：
相关入口/模块：
当前已知事实：
计划修改文件：
单测与验证命令：
真实环境验收：
成功标准：
失败/回滚方案：
文档更新：
```

AI 输出任务结论时必须使用：

- 已完成：确实完成且有证据的内容
- 已验证：测试/真机/端到端证据
- 未完成：剩余工作
- 已知限制：当前不能承诺的内容
- 下一步：按 P0-P3 排序的最小下一步

## 10. 文档维护规则

- `README.md`：项目介绍（定位、架构、目录结构、路线图、合规）+ §9 文档索引。
- `docs/*.md`：正文分册（原 README 的 §9 快速开始 / §11 去 CGO 化 / §12 确定性执行计划），
  **章节编号沿用原 README 编号**；「README §x.y」这类旧引用按 README §9 文档索引对应到文件。
- `DEVELOPMENT_PLAN.md`：目标、阶段、约束、验收、风险和 AI 协作规范。
- `AGENTS.md`：AI 协作须知速查（红线、坑、闭环），面向所有在本仓库工作的 AI 智能体。
- `.workbuddy/memory/YYYY-MM-DD.md`：当天已完成工作的事实记录。
- 每次阶段状态变化、关键实验结论、接口约束或发布规则变化，都必须同步更新本文。
- 不把实验猜测写成结论；所有“已验证”必须能指出测试、日志、截图或真实运行证据。
