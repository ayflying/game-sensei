# AGENTS.md — AI 协作须知（game-sensei）

> 面向所有在本仓库工作的 AI 智能体。**开始任何工作前必须先读本文，再读 `DEVELOPMENT_PLAN.md` 与 `README.md` 对应章节。**
> 本文只写“AI 容易搞错、且必须记住”的事项；完整规范以 `DEVELOPMENT_PLAN.md` 为准，二者冲突时以 `DEVELOPMENT_PLAN.md` 为准。

---

## 0. 四条最重要的红线

### 0.1 知识归档分界（最常搞错，务必分清）

学到的东西分两处放，**别放反**：

| 内容 | 去处 |
|---|---|
| **单一游戏**的玩法、界面状态机、坐标、攻略、数值、关卡 | **只进 ima 知识库**（「游戏」KB `7504129723208370` → **该游戏自己的文件夹**）。**不写进代码仓库。** |
| **通用**的游戏类型共同规则/节奏/决策模式、可复用的操作与工程经验 | **进代码仓库** `outputs/game-sensei-游戏类型知识库.md`（按类型分节累积） |

**一句话判断标准：换一款同类游戏还能不能用 —— 能 → 仓库；不能 → 知识库。**

⚠️ **项目工作记忆不进 ima**：`.workbuddy/memory/` 是「续接上下文」用的，内容与仓库重复，传进 ima 等于把网盘当仓库。

#### 0.1.1 知识库是**知识库**，不是网盘（2026-09-15 定档）

**只写不读 = 冷备份。** 此前 100% 只用写入通道、从不在动手前检索，等于把知识库当网盘用。规矩：

- **先搜后写**：开新游戏 / 遇新问题，**先 `search_knowledge(knowledge_base_id, query)` 取先验**，再上网找攻略、再动手。**同厂同域优先**（Sparkle 笔记里就有 YoYa 系的 UI 范式）。
- **要正文用 `fetch_media_content(media_id)`**（返回整篇 markdown 原文，实测无损）；查目录才用 `get_knowledge_list`。两者别混。
- `search_knowledge` **能跨文件夹召回**，返回 `highlight_content` 高亮片段 + `parent_folder_name`。它比仓库检索强的地方是**跨游戏类比**——查 Sparkle 判胜会把「洛克王国-战斗判据修正」带出来，而那篇讲的正是同源的判据坑。
- **笔记必须留可检索钩子**：标题与正文要含**游戏名 / 包名 / 症状词**，否则搜不到。
- 读写两条通道的完整用法见用户级技能 `ima-kb-ingest`。

其余约定：

- 标题格式：`游戏名-文档名`（如 `洛克王国：世界-攻略合集`、`Sparkle-新手教程流程与界面实测笔记`）；项目级文档用 `game-sensei-文档名`。
- ⚠️ **入库前必须确认 `folder_id`**：省略会默认落 `game-sensei` 过渡夹 ⇒ 单游戏笔记极易躺错位置。**「入库成功」≠「落在正确文件夹」**，报告里要写明「暂存」与待建夹清单，别让它静默躺着。
- **⚠️ 没有专属文件夹的游戏 → 一律放「其他游戏」夹**（`folder_7505468754758375`，2026-09-15 安酱建）。**别把单游戏笔记留在 `game-sensei` 过渡夹**；等该游戏攒够内容再请安酱建专属夹、届时移出。
- **「游戏」KB `7504129723208370` 现有文件夹**：

  | 夹 | folder_id | 放什么 |
  |---|---|---|
  | `洛克王国：世界` | `folder_7504762157139963` | 单游戏 |
  | `Sparkle` | `folder_7505096032126399` | 单游戏 |
  | `Yoya Star` | `folder_7505441466620084` | 单游戏（App 里写的是 "Yoya Star"，不是 "YoYa"） |
  | `其他游戏` | `folder_7505468754758375` | **无专属夹的游戏**（如解忧梦幻岛） |
  | `回收站` | `folder_7505466338840832` | MCP 无删除能力 ⇒ 清理 = 移入此夹，等安酱手删 |
  | `game-sensei` | `folder_7504129807094803` | **仅项目级**（`game-sensei-*` 前缀）；单游戏笔记不放这 |

- ⚠️ 别写错库：`folder_7504129186334348` 属于「微信用户的知识库」，是早期误存位置。
- **不要用 `import_urls` 存网址**——必须 WebFetch 提取正文、整理成 markdown 再入库。
- ima MCP **没有建文件夹 / 改名 / 删除**的工具；文件夹由用户在 App 手动建，误存条目也要用户手动删。
- **计数与 `folder_id` 过滤都不可信**：传 `folder_id` 会混入兄弟文件夹条目，`file_number` / `total_size` 也不作数 ⇒ 核验只能**按标题找到那条、再读它的 `parent_folder_id`**。
- 玩新游戏前先上网找**最新攻略**，抓正文、整理、入库再开玩。
- 完整入库流程见用户级技能 `ima-kb-ingest`（含 COS 签名上传脚本）；中转文件放 `.workbuddy/ima-uploads/`（已 gitignore），**cred.json 用完立即删**。

### 0.2 绝不用 `git add -A` / `git add .`

安酱可能在别的会话/窗口**并发**改同一工作区（2026-09-11 出现过并发写入 `internal/video/`）。
**必须逐个列出本次涉及的文件路径暂存**。提交只带本次涉及文件，中文 commit，验证通过后当轮自动 push。

### 0.3 区分「已实现 / 已单测 / 已本地验证 / 已真机验证 / 已发布」

**不以代码存在代替功能完成**。结论必须来自真实实测输出；调试服、测试环境、正式环境必须分清。

### 0.4 图片理解：本地优先，主对话默认只接收文字（2026-09-18）

**已确认问题**：用户在中转侧确认大量内嵌图片导致请求体超限。提供的统计截图显示：最大请求
8.39 MB，其中内嵌 JPEG 7.90 MB（94.1%）。**单图压缩不能解决历史图片累计**；分轮读图也不代表旧图
已经退出请求。该样本不是平台统一上限；其他 400 仍须根据错误正文与中转日志判断。

**默认路径：原图留本地 → 本地确定性检查或 Ollama 识别 → 返回文字证据；仅失败时裁剪、压缩后升级大模型。**

#### A. 原图本地理解（默认，禁止先读进主对话）

1. 原始截图保留，不覆盖、不删除；默认不为本地识别预先进行有损压缩。现有截图工具自动生成小图不受影响，
   但本地识别优先取原图。**不要先用图片读取工具“看一下再决定”**，那已经把图片放进主对话了。
2. 已有可靠的颜色比例、局部帧差、模板或本地 OCR 能回答的问题，优先直接计算；复杂语义再交本地 Ollama。
   只通过屏幕视觉判断，不引入游戏内存、封包等非视觉信息。
3. 本项目已知 Ollama 地址为 `http://127.0.0.1:11435`。使用前检查 `/api/tags`，确认实际存在且支持视觉的模型；
   优先已验证的 `qwen3.5:9b`，不可假定模型或服务始终可用。不得静默改用远程服务，也不自行下载模型。
4. 本地脚本从磁盘读取原图，将图片编码后直接发给 Ollama `/api/chat`；**脚本输出只包含文字结果**，
   禁止打印 Base64、data URL、整份请求体或通过工具返回图片。请求只携带当前必要帧，不累计历史图。
   `think:false` 放请求顶层，设置有限超时（默认 60 秒）与输出预算；复杂任务可明确调整，禁止无限等待。
5. 每次问明确问题，而非泛泛“描述截图”。返回：`图片路径/帧时间、任务问题、观察事实、原文/数值、
   证据区域、结论、无法确定项`。区分观察与推断；模型自报置信度不是正确性证明，空输出/截断输出不算成功。
6. 小字模糊、关键字段缺失、结论矛盾或答非所问：先从原图无损裁剪目标区域，补必要上下文和具体问题，
   本地最多再试 1 次。原图超过本地模型资源限制时可裁剪或适配尺寸，但保留原图并记录处理；不要反复发送同一输入。
   服务不可达、模型不支持视觉、超时或显存不足也属于明确失败，记录原因后走升级路径。

#### B. 升级大模型（有条件兜底，不是常规读图路径）

1. 只有本地无法回答任务所需问题，或关键结论缺乏可核验证据时才升级；先说明失败原因和待核实问题。
   胜负判定、精确坐标等不得仅因本地模型“很自信”就执行；坐标须保留裁剪偏移、缩放比例并映射回原图。
2. **先裁剪目标区域，再压缩为 `*.s.jpg`**；必要时保留少量布局上下文，禁止拼成长图规避张数预算。
   优先使用 `cmd/shot` 的既有压缩能力；裁剪工具使用前确认实际存在，不引用虚构脚本。
3. 默认每次升级 **1 张**，必须对照时最多 **2 张**；单张目标不超过 **200 KiB**、本次合计不超过 **400 KiB**，
   编码前检查实际文件字节数。这是项目保守预算，不是上游硬限制，也不保证整个历史请求低于上限。
   小字压缩后不可读时优先缩小裁剪范围，不反复降低画质；确需超预算须说明原因并由用户确认。
4. **优先交给独立、短生命周期的图片识别子会话**，只传问题、必要文字背景和压缩图路径；只接收文字证据。
   子会话同样遵守图片预算；若会继承主对话完整历史，则不能认为它隔离了旧图。无法确认隔离时如实说明。
5. 不重复读取同一图片；缓存以图片内容哈希、问题、模型及识别配置为键，界面变化后重新判读。
   连续截图只送关键帧/变化区域；动态对比最多带必要的前后两帧，不上传整段帧序列。

#### C. 上下文与执行边界

- 主对话保留“结论 + 证据文字 + 本地图片路径”，不保留图片编码；原图在本地可追溯。
- 用户已直接贴入对话的图片可能已进入上下文，无法靠本规则撤回；不要为了检查而重复附图。
- **文字摘要、停止读图、清空本地文件，都不等于删除客户端历史图片**。若旧会话持续超限，
  用纯文字交接摘要开启新会话，或使用客户端确实支持的图片历史裁剪机制；禁止宣称规则已自动清掉旧图。
- 请求体超限时禁止原样重试；记录请求 ID、字节数/图片数（能获取时）及失败层级，不打印敏感请求正文。
- 本规则是 AI 协作约束，**不代表已经实现自动图片路由、请求拦截或历史图片清理**；不得把规则落盘说成端到端修复。

既有工具：`shot -o _cal.png` 自动保留原图并生成 `_cal.s.jpg`；`shot -front` 看前台包名；
`shot -pack .workbuddy/demos` 可给历史截图补小图。小图只在必要升级时进入大模型，不作为默认读图入口。

---

## 1. 动工前必读

- `DEVELOPMENT_PLAN.md` —— 主约束文档，含 AI 执行总规则（先理解后修改 / 小步变更 / 不覆盖用户工作：**不得 reset、checkout、clean 或删除探测产物** / 完成后闭环）。
- `README.md` —— **只放项目介绍**（定位 / 自学习闭环 / 角色 / 技术栈 / 目录结构 / 路线图 / 设计决策 / 合规）+ §9 文档索引。
- `docs/` —— **正文分册**（原 README 的三大块）：`quickstart` / `backends` / `action-layer` / `teacher-demo-video` / `cgo-free` / `runtime-notes` / `student-training` / `nrc-field-notes` / `plan-engine` / `plan-sparkle` / `reference`。
  **分册里的章节编号沿用原 README 编号**（如 `docs/plan-engine.md` 里是 `12.1`~`12.5`），所以「README §11.16」这类旧引用按 [README §9 文档索引](README.md#9-文档索引) 对应到文件即可。
  **已确认功能的接口、参数、实测数据、限制，必须同步更新对应文档。**
- `outputs/game-sensei-学习能力开发计划.md` —— 只看学习路线图（P0/P1/P2）时读它。

---

## 2. 硬约束（不可违反）

- **零 CGO**：核心零 CGO（`CGO_ENABLED=0`）。不引 robotgo / onnxruntime_go 到核心；推理走 LazyDLL 挂 dll 或独立子进程。
- **只视觉 + 模拟输入**：不读内存、不接封包、不做绕过反作弊。
- **学生模型跑实时回路；教师模型只能异步**，不得阻塞实时控制。
- **PC 与安卓必须走统一 backend 和动作协议**；游戏差异放进 profile，不在核心逻辑硬编码。
- 仓库根 `/*.png /*.jpg /*.txt /*.status /*.ls` 已 gitignore（真机标定产物随时可重跑）。
- `.workbuddy/` 已 gitignore（含实测截图、评测报告、ima 上传中转文件）。**不要删这个目录**。

---

## 3. 架构速查

### 3.1 双后端

`cmd/helper/backend.go` 的 `backend` 接口（`Grab/GrabColor/Resolve/Apply/Profile/Size/Describe/Live/Close`）统一 PC 与安卓。

- `pc`（默认）：`internal/capture`（GDI BitBlt）+ `internal/input`（SendInput），30 FPS。
- `android`：`internal/android`（`adb.exe`），**默认自动降为 5 FPS**（screencap ≈0.94s/帧）。
- **横屏陷阱**：`ScreenSize()` 必须以缓存截图尺寸为准，物理 1200×2608 横屏下坐标系是 2608×1200。

### 3.2 动作空间分层（最重要的设计约束）

**L1 语义层（跨游戏通用，模型输出）↔ L2 执行层（平台相关，档案展开）**，中间由 `internal/game` 的 GameProfile 翻译。**换游戏只换一份 JSON 档案，Go 代码不动。**

- L1：`MOVE dir=前` / `PRESS name=jump` / `TAP` / `SWIPE` / `HOLD` / `KEY` / `WAIT`
- L2：`JOYSTICK` / `KEY`（Dur>0 = 按住）/ `TAP`（像素）/ `MouseMove`
- **模型只做 8 选 1 的方向分类**；摇杆中心与幅度是设备常量，交给档案。
- `Profile.Resolve` **幂等**：已是 L2 的动作原样返回，后端可无脑「先 Resolve 再执行」。
- 方向别名里单字母 `w/a/s/d` 按 **WASD 键位语义**（w=前），不按罗盘缩写。
- 斜向推杆用 1/√2 分量，保证八向幅度等长。
- 档案字段名拼错**会立刻报错**（`DisallowUnknownFields`），不许静默退化。

### 3.3 加一款新游戏（无需改 Go 代码）

1. `python tools/grid_overlay.py shot.png --region x0,y0,x1,y1 --zoom 2 -o out.jpg` 读坐标
2. 写 `internal/game/profiles/<游戏名>.json`（或放 `./profiles/` 覆盖内置）
3. `go run ./cmd/helper -game <档案名> -frames 100` dry-run 验证
4. `-game` 会带出档案里的 `package`，可省 `-app`

内置档案：`nrc`（洛克王国）/ `mobile_generic` / `pc_generic`。

---

## 4. 已踩过的坑（别重复踩）

- **Ollama `think` 开关必须放 `/api/chat` 请求顶层，不能放进 `options`**。放错位置 Ollama 直接忽略，表现为「关不掉思考、正文为空、打满 num_predict」。这是本项目最容易误判的一个坑。
- qwen3 系推理在 `message.thinking`、正文在 `message.content`，**两个字段都要兜底**；`num_predict ≥ 1500`。
- **小模型会逐字节照抄提示词里的示例**。写动作协议示例必须用 `<占位符>`，绝不放具体数字。
- **浮窗挂死**：overlay Hide→BitBlt→Show 与 GDI 互斥挂死，`-overlay` 默认 false（README §11.7）。
- **锁屏坑**：LockApp（类名 `Windows.UI.Core.CoreWindow`）在前台时 SendInput 点击落锁屏，GDI 会话级失效。**锁屏下窗口位置设置仍生效，但置顶会被前台锁拒绝**——先跑 `cmd/focusdbg` 诊断，别盲目重试；用 `cmd/unlock-watch` 等解锁后自动置顶。
- **窗口域是动态跟踪**：`SetWindowRegion(rect, keyword)` 每帧现查客户区，`Actuator.OffsetFunc` 每帧现查偏移——窗口拖动/缩放不失效。别改回静态缓存。
- **退出必须调 `ReleaseAll()`**，否则键盘卡在按下状态。
- **MuMu 模拟器**：自带 adb 版本旧，用 Android SDK 版 `adb.exe`；模拟器内 adb 端口 `127.0.0.1:16384`。详见用户级技能 `mumu-emulator-control`。
- **图片请求体超限**：原图优先在本地 Ollama 理解，主对话只收文字；本地失败才裁剪、压缩并限量升级。不要仅凭 `400 Unable to read request body` 推定所有故障同因，详见 §0.4。

---

## 5. 完成闭环（每次改动）

```bash
gofmt -w <本次修改的.go文件>
go test ./...
go vet ./...
go build ./...
git diff --check
git status
```

- 只暂存本次涉及文件（见 §0.2），中文 commit，push。
- 真实设备/安卓功能必须补真实环境验证。
- 最终报告列出：改动文件、测试结果、真实验证环境、指标、未解决问题、是否可发布。
- **AI 输出任务结论必须区分**：已完成（有证据）/ 已验证（测试/真机/端到端）/ 未完成 / 已知限制 / 下一步。

---

## 6. 记忆与工具

- 项目日志：`.workbuddy/memory/YYYY-MM-DD.md`（**append-only**，当天已完成的**事实**）。
- 长期项目约定：`.workbuddy/memory/MEMORY.md`（就地更新）。
- 调试工具集 `cmd/`：`winlist`（列窗口）、`winshot`（置顶+截全屏）、`winclick`（置顶+点击）、`focusdbg`（前台 Win32 类名诊断）、`unlock-watch`（等解锁后置顶）、`overlay-test`。
- 标定工具 `tools/`：`grid_overlay.py`（坐标读数）、`detect_state` / `runstats` / `watch` / `movetest`（洛王国真机跑批）。
- CodeGraph：`C:/Users/ay/AppData/Local/codegraph/current/bin/codegraph.cmd`，重建索引 `codegraph init -i`；`.codegraph/` 已 gitignore。
- 老师模型本地 Ollama 项目实例：端口 **11435**，模型库 `.ollama/models`，启动 `tools/serve_ollama.sh`。
- 探索辅助脚本（临时工具，`.workbuddy/` 已 gitignore）：`.workbuddy/sparkle/sp.py` ——
  `shot <名>`（截图**并自动压出 `.s.jpg`**）/ `tap` / `tapraw` / `swipe` / `front` / `pack [目录]`（批量压缩历史截图）/ `newest`（输出最新一张的可读路径）/ `do "序列"`。
  **原图交本地 Ollama，主对话默认只收文字；本地失败才读取压缩后的 `.s.jpg`**（见 §0.4）。
