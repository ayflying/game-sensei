# 洛克王国:世界 真机跑批报告

> 2026-09-21 · MIX 3（`ecbff3a5`，横屏 2340×1080）· 工具 `tools/nrc_soak.py`
> 原始数据：`.workbuddy/evidence/nrc/20260921-soak/soak.json`（逐轮可复算）

## 一句话结论

**这条链路是确定性的，不是"偶尔能成"**：六轮正式跑批合计 **74 轮**，
**链路成功率 100%、复位成功率 74/74**，且**没有累积漂移或性能退化**
（20 轮长跑里第 19/20 轮反而最快）。目标已从 **1 个图标扩到 4 个**（每轮轮换），
四个目标各自 4/4 通过。单轮耗时经两阶段提速从 **55.8s 降到 21.1s（-62%）**，
其中**游戏侧真正的交互不足 1 秒**，其余全是取证链条开销。

## 跑的是什么

一轮完整闭环（只用**退出路径已实测无损**的动作，不破坏游戏状态）：

```
抓帧 → 全库定位(24 条图标模板) → 点目标图标 → 抓帧验证反应 → 复位回地图
```

判"点击成功"的标准是**帧差达标 + OCR 新增文字**双判据，不是"命令返回 0"。

## 六轮结果

| 跑批 | 起点 | 轮数 | 成功率 | 复位 | 平均单轮 |
|---|---|---|---|---|---|
| ① 正常起点连续跑（单图标） | 地图界面 | 10 | **100%** | 10/10 | 32.7s |
| ② 异常起点（面板态，故意） | 面板态 | 10 | **100%** | 10/10 | 34.1s |
| ③ 长跑复核（单图标） | 地图界面 | 20 | **100%** | 20/20 | 32.8s |
| ④ **多图标轮换**（4 目标 × 4 轮） | 地图界面 | 16 | **100%** | 16/16 | 32.1s |
| ⑤ **提速后基线**（4 目标 × 4 轮） | 地图界面 | 16 | **100%** | 16/16 | **21.2s** |
| ⑥ **强制重抓验证**（慢帧阈值 500ms） | 地图界面 | 2 | **100%** | 2/2 | 67.0s |

④⑤两轮**逐轮帧差恒为 17.85 / 30.98 / 42.19 / 3.24**（四个目标各 4 次全同）—— 可完全复现。
⑥ 是**故意让每帧都判慢帧并重抓**的加严测试：成功率仍 2/2、帧差与 ⑤ **完全一致**
⇒ 重抓只影响耗时，不影响判据。

## 多图标：从 1 个目标扩到 4 个

单图标跑 40 轮 100% 只证明"**这一个**点得动"。扩目标前必须先逐个实测退出路径
（点进去出不来比点不动更糟）：

| 目标 | 点击反应 | 实测帧差 | 退出方式 | 4 轮结果 |
|---|---|---|---|---|
| 眠枭庇护所(地图UI) | 区域进度面板（风眠省 15/15 + 子区域列表） | 恒 17.88 | ✕ ×1 → panel→world→map | **4/4** |
| 皇家招待所 | 标记编辑态（"标记（点击修改名称）"8/270） | 恒 31.07 | ✕ ×1 → panel→world→map | **4/4** |
| 家园 | 家园信息浮层（等级 25 / 舒适度 / 居住精灵） | 恒 42.19 | ✕ ×1 → panel→map | **4/4** |
| 皮卡月刊 | **只有名字标签**（它不是按钮，是地图标注点） | 恒 2.01 | **再点同位置一次** | **4/4** |

每个目标重复 4 次，**帧差逐轮完全一致** —— 说明"点一次就开面板"在不同目标上都是确定的。

> 顺带发现**复位路径不止一种**：家园点一次 ✕ 就回地图（`panel→map`），
> 而眠枭庇护所(地图UI)、皇家招待所要先落大世界再开地图（`panel→world→map`）。
> ✕ 的语义取决于上层是哪种浮层，不能假设"一次 ✕ 回地图"。

## 单轮 21.2 秒花在哪（提速后口径）

| 阶段 | 耗时 | 说明 |
|---|---|---|
| 抓帧 | 7.1s | 3 次/轮 × ~2.0s（含复位过程中的抓帧） |
| 全库定位 | 4.2s | 24 模板单尺度；**与 base 的 OCR 并行** |
| 点击与反应验证 | 3.8s | 含"点后等 1.2s 再抓帧"的稳定窗口 |
| 复位流程 | 8.4s | 内含它自己的抓帧与点击（与上面"抓帧"行**有重叠**） |
| **单轮总** | **21.2s** | |

> 各项之和大于总时间：定位与 OCR 并行（时间重叠）、复位段含抓帧（重复计入）。

⇒ 仍然**几乎全是工具侧取证/识别的固定开销**，游戏侧真正的交互（4 次点击注入）
本身不足 1 秒；表里"点击与验证"主要是**为确认结果而留的稳定等待窗口**。
所以"跑一轮 21 秒"不代表游戏卡，而是取证链条的开销。

**提速三阶段（判据全程未动）**：

| 阶段 | 手段 | 单轮 |
|---|---|---|
| 起点 | — | 55.8s |
| ① 去重复劳动 | 复用定位结果判态 / 帧差判态 / 复位复用点击后那帧 | 32.5s |
| ② 挪开 OCR 与定位 | `ocr -serve` 常驻（冷启动整批只付一次）+ base 的 OCR 与定位**并行** | **21.1s** |

游戏已复位到干净地图界面，诊断用的一次性帧已清理。

## 四个"给出漂亮假数据"的坑（都已修）

### 坑①：toggle 图标 + 盲点两次 = 帧差 0.00

该图标点一次开面板、再点一次关面板。早期默认**点两次** ⇒ 开了又关 ⇒
逐像素回到原状（帧差 **0.00**），看起来和"点击根本没发出去"一模一样。
判定实验（同一脚本内交替点、每步只点一次）：点地图中部 23.19、点目标 **18.42**、
再点目标**也是 18.42**（与起点同画面）。
⇒ 修法：**点一次 → 抓帧 → 帧差 <3.0 才补点一次**（自适应）。

> 附带更正：排查时我一度把这条假象归因于"沙箱拦截多层 python 调用"，
> 并据此改了 3 处代码 —— **那是错的**，真因只是两次对照实验的点击次数不同。
> 教训：拿"点不动"当结论前先固定点击次数做对照；诊断脚本一次只变一个变量。

### 坑②：自证式判据 —— 复位抓帧覆盖了基准帧

复位"是否已回到地图"用帧差与基准帧比，而复位自己的抓帧**与基准帧共用文件名**
（都不带轮次）⇒ 复位抓帧**覆盖**了基准帧 ⇒ 变成**自己跟自己比 ⇒ 帧差恒 0**
⇒ 永远判"已复位"。更糟：错误状态被下一轮当基准继承，**一路错且不报错**。
⇒ 修法：帧名按轮次隔离 + 同源比较直接返回 `None`（逼调用方走真判据）+ 阈值按语义收紧。

**这个坑只在"异常起点"测试里暴露** —— 正常起点连续跑 10 轮全 100%，
因为起点守卫那段代码**一次都没被执行过**。所以跑批必测两类起点。

### 坑③：自适应补点亲手把成功改写成失败（同一个 toggle 陷阱换了个马甲）

按坑①加了"首点没变化就补点"之后，多图标首版跑批里**皮卡月刊帧差 0.00 判"无反应"**。
它不是点不动，而是它的反应**本来就很小**（它是 toggle 名字标签，点一次只弹一个小标签）：

```
点一次 → 出标签（帧差 2.04）
       → 2.04 < 补点门槛 3.0 ⇒ 判"首点被吞" ⇒ 补点一次
       → 标签被收回 ⇒ 帧差回到 0.00 ⇒ 判"点了没反应"（与"完全没点到"一模一样）
```

⇒ 修法：门槛按**目标类型**给（面板类 8.0 / 标签类 1.5），且标签类**直接关掉补点**
（2.04 vs 1.5 只差 0.5，靠调门槛擦边不稳）。

> 通用教训：**凡"自动重试/自动纠错"的通用保护，作用在边缘目标上时，
> 优先怀疑它是不是在把成功改成失败**，而不是先怀疑链路坏了。

### 坑④：抓帧"兜底"把失败静默变成"拿到旧帧"

16 轮长测里有一轮单轮 64.8s，其中**一次抓帧花了 39.2s**（同轮其他抓帧 2.1s），
而且那帧帧差 **60.64**、与同目标其他轮的 30.98 明显不同 ——
**慢帧通常抓在画面过渡态上，内容不可信**。原实现只把耗时记进统计、不做兜底。

查这个问题的过程中，用离线单测又挖出一个**更隐蔽的真 bug**：抓帧函数在拿不到工具
回传路径时会兜底去读约定文件名 `<tag>.png`，而那是**同名复用**的文件 ——

```
抓帧失败 → 兜底命中"上一轮残留的旧帧" → 当成"本次结果"返回
        ⇒ 后续点击/复位的判据全建在旧画面上，rc=0、不报任何错
```

⇒ 修法三件（判据不变）：子进程超时 60s、单次 >8s 判慢帧**丢弃重抓一次**、
**新鲜度校验**（帧 mtime 必须晚于本次抓帧开始时间）。配套：被丢弃的慢帧挪到
`<tag>.png.slow`（否则下次失败时仍会被兜底捡回来）、被丢弃的耗时单独记账。

> 通用教训：**任何"文件/缓存/上次结果"式兜底，都要能回答"这份数据是不是本次产生的"。**
> 这类 bug 的共同特征：**不报错、指标还好看**，只有**把失败路径单独测一遍**才会露出来。

## 产物

- 工具：`tools/nrc_soak.py`（跑批，`--targets` 选目标）、`tools/icon_match.py`（定位+点击，`--tap-verify`）
- 图标库：24 条模板（`.workbuddy/nrc/icon_atlas/library/`，本地资产，未入库 git）
- 证据：`.workbuddy/evidence/nrc/20260921-soak/soak.json`（单图标 40 轮）、
  `.workbuddy/evidence/nrc/20260921-soak-multi/soak.json`（多图标 16 轮）
- 方法论文档：`docs/nrc-icon-atlas-method.md` §5.8 / §5.9、`docs/nrc-field-notes.md` §11.30 / §11.31

## 下一步（按性价比）

1. **继续扩目标，但优先级降低**：4 个固定 UI 已够覆盖"面板类 / 标记类 / 标签类"三种反应形态；
   再加目标前先想清"能多回答什么问题"，否则只是重复。
2. **提速已走完两阶段（55.8s → 21.1s，-62%）**：OCR 常驻 + 定位并行。
   剩下的固定开销主要是抓帧（1.9s × 4 次）与全库定位（3.1s）。
   继续提速要**换通道**（抓帧降传输量 / 定位缩 ROI），不要抠判据 ——
   这仍是"玩起来更顺"的最大瓶颈，但注意**游戏侧真正的交互本身不足 1 秒**。
3. **把"反应类型 + 退出路径"写进图标库**：跑批用的退出策略目前硬编码在 `TARGETS` 表里，
   库里还没有 `reaction`（panel/label/marker/dead）与 `exit`（close/retap）字段。
4. 图标库 8 条"单帧出现" + 2 条"未出现"待换帧补验。
