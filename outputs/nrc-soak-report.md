# 洛克王国:世界 真机跑批报告

> 2026-09-21 · MIX 3（`ecbff3a5`，横屏 2340×1080）· 工具 `tools/nrc_soak.py`
> 原始数据：`.workbuddy/evidence/nrc/20260921-soak/soak.json`（逐轮可复算）

## 一句话结论

**这条链路是确定性的，不是"偶尔能成"**：三轮正式跑批合计 **40 轮**，
**链路成功率 100%、复位成功率 40/40**，单轮稳定在 31~35 秒，
且**没有累积漂移或性能退化**（20 轮长跑里第 19/20 轮反而最快）。

## 跑的是什么

一轮完整闭环（只用**退出路径已实测无损**的动作，不破坏游戏状态）：

```
抓帧 → 全库定位(24 条图标模板) → 点「眠枭庇护所(地图UI)」→ 抓帧验证反应 → 复位回地图
```

判"点击成功"的标准是**弹出面板**（帧差 + OCR 新增文字双判据），不是"命令返回 0"。

## 三轮结果

| 跑批 | 起点 | 轮数 | 成功率 | 复位 | 平均单轮 |
|---|---|---|---|---|---|
| ① 正常起点连续跑 | 地图界面 | 10 | **100%** | 10/10 | 32.7s |
| ② 异常起点（面板态） | 面板态（故意） | 10 | **100%** | 10/10 | 34.1s |
| ③ 长跑复核 | 地图界面 | 20 | **100%** | 20/20 | 32.8s |

三轮**逐轮帧差恒为 18.42、新增文字恒为 12 条、定位命中恒为 21 条** —— 可完全复现。

## 单轮 32.7 秒花在哪

| 阶段 | 耗时 | 性质 |
|---|---|---|
| 复位流程（抓帧/点击/判态） | 11.3s | 工具侧取证 |
| 抓帧（4 次 × 1.9s） | 7.8s | 工具侧取证 |
| OCR 与解析开销 | 6.6s | 工具侧识别（整帧 OCR 3.6s/次） |
| 全库定位（24 模板） | 3.3s | 工具侧识别 |
| 点击与验证 | 3.7s | **游戏交互** |

⇒ **约 89% 是工具侧取证/识别的固定开销**，游戏侧真正的交互（4 次点击注入）本身不足 1 秒，
图里的"点击与验证"主要是**为确认结果而留的稳定等待窗口**。
所以"跑一轮 33 秒"不代表游戏卡，而是取证链条的开销。

## 两个"给出漂亮假数据"的坑（都已修）

### 坑①：toggle 图标 + 盲点两次 = 帧差 0.00

该图标点一次开面板、再点一次关面板。早期默认**点两次** ⇒ 开了又关 ⇒
逐像素回到原状（帧差 **0.00**），看起来和"点击根本没发出去"一模一样。
判定实验（同一脚本内交替点、每步只点一次）：点地图中部 23.19、点目标 **18.42**、
再点目标**也是 18.42**（与起点同画面）。
⇒ 修法：**点一次 → 抓帧 → 帧差 <3.0 才补点一次**（自适应）。

> 附带更正：排查时我一度把这条假象归因于"沙箱拦截多层 python 调用"，
> 并据此改了 3 处代码 —— **那是错的**，真因只是两次对照实验的点击次数不同。
> 教训：拿"点不动"当结论前先固定点击次数做对照；诊断脚本一次只变一个变量。

### 坑②：自证式判据 —— 复位抓帧覆盖了基准帧

复位"是否已回到地图"用帧差与基准帧比，而复位自己的抓帧**与基准帧共用文件名**
（都不带轮次）⇒ 复位抓帧**覆盖**了基准帧 ⇒ 变成**自己跟自己比 ⇒ 帧差恒 0**
⇒ 永远判"已复位"。更糟：错误状态被下一轮当基准继承，**一路错且不报错**。
⇒ 修法：帧名按轮次隔离 + 同源比较直接返回 `None`（逼调用方走真判据）+ 阈值按语义收紧。

**这个坑只在"异常起点"测试里暴露** —— 正常起点连续跑 10 轮全 100%，
因为起点守卫那段代码**一次都没被执行过**。所以跑批必测两类起点。

## 产物

- 工具：`tools/nrc_soak.py`（跑批）、`tools/icon_match.py`（定位+点击，`--tap-verify`）
- 图标库：24 条模板（`.workbuddy/nrc/icon_atlas/library/`，本地资产，未入库 git）
- 证据：`.workbuddy/evidence/nrc/20260921-soak/soak.json`
- 方法论文档：`docs/nrc-icon-atlas-method.md` §5.8 / `docs/nrc-field-notes.md` §11.30

## 下一步（按性价比）

1. **把跑批从"单图标"扩到"多图标"** —— 前提是先把各图标的**退出路径**实测出来
   （家园会切地图层级、皮卡月刊是资讯入口，目前只用它们做定位、不做点击）。
2. **提速要换通道，不要抠判据**：抓帧降传输量 / OCR 换常驻进程（省每次 3s 引擎启动）/ 定位缩 ROI。
3. 图标库 8 条"单帧出现" + 2 条"未出现"待换帧补验。
