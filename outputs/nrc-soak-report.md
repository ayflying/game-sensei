# 洛克王国:世界 真机跑批报告

> 2026-09-21 · MIX 3（`ecbff3a5`，横屏 2340×1080）· 工具 `tools/nrc_soak.py`
> 原始数据：`.workbuddy/evidence/nrc/20260921-*`（十七批的证据目录，清单见文末「产物」）；
> 每个目录均含 `soak.json` + **带时间戳的存档**（固定名那份会被下次跑批覆盖）

## 一句话结论

**这条链路是确定性的，不是"偶尔能成"**：十七批正式跑批合计 **208 轮**，
**有效轮成功率 100%**（判了点击的轮次全部成功），且**没有累积漂移**
（30 轮长跑里前 5 轮均 21.7s、后 5 轮均 21.3s）。目标已从 **1 个图标扩到 4 个**（每轮轮换），
30 轮里四个目标各自 7~8 轮全部通过、**逐轮帧差收敛到同一个值**。
单轮耗时经四个阶段从 **55.8s 降到 20.8s（-63%）**，
其中**游戏侧真正的交互不足 1 秒**，其余全是取证链条开销。

> 全部 208 轮里只有 **3 轮"目标未在屏 ⇒ 未决"**，且**全部出现在两处判据缺陷期间**
> （坑⑤ 叠加层误判、坑⑥ 过渡帧误判），修完即消失。
> 这份报告**最有价值的不是"184 轮"，而是五处"指标全绿但实际是假的"**
> （坑④ 旧帧冒充新帧、坑⑤ 叠加层、坑⑥ 过渡帧、坑⑦ 汇总里的 `None`、
> 坑⑧ **为修坑⑥ 而新引入的锚点快路径**把区域面板判成地图）——
> 它们**只在失败路径 / 异常起点 / 汇总异常值上才暴露**，正常起点连跑多少轮都测不出来。
> 其中坑⑧ 是**自己修 bug 时亲手造出来的新 bug** ⇒
> **修完一处判据缺陷，必须重跑它当初暴露的那个起点**，而不是"正常起点全绿"就收工。

## 跑的是什么

一轮完整闭环（只用**退出路径已实测无损**的动作，不破坏游戏状态）：

```
抓帧 → 全库定位(24 条图标模板) → 点目标图标 → 抓帧验证反应 → 复位回地图
```

判"点击成功"的标准是**帧差达标 + OCR 新增文字**双判据，不是"命令返回 0"。

## 十七批结果

| 跑批 | 起点 | 轮数 | 成功率 | 复位 | 平均单轮 |
|---|---|---|---|---|---|
| ① 正常起点连续跑（单图标） | 地图界面 | 10 | **100%** | 10/10 | 32.7s |
| ② 异常起点（面板态，故意） | 面板态 | 10 | **100%** | 10/10 | 34.1s |
| ③ 长跑复核（单图标） | 地图界面 | 20 | **100%** | 20/20 | 32.8s |
| ④ **多图标轮换**（4 目标 × 4 轮） | 地图界面 | 16 | **100%** | 16/16 | 32.1s |
| ⑤ **提速后基线**（4 目标 × 4 轮） | 地图界面 | 16 | **100%** | 16/16 | **21.2s** |
| ⑥ **强制重抓验证**（慢帧阈值 500ms） | 地图界面 | 2 | **100%** | 2/2 | 67.0s |
| ⑦ **提速后长跑**（4 目标轮换 8/8/7/7） | 地图界面 | 30 | **100%** | 30/30 | **21.5s** |
| ⑧ **异常起点·修复前**（家园浮层） | 家园浮层 | 12 | ⚠️ 100%（10/10）**假绿** | ⚠️ **10/12** | 23.6s |
| ⑨ **异常起点·修复后**（家园浮层） | 家园浮层 | 12 | **100%** | **12/12** | 27.7s |
| ⑩ **抓帧通道 raw**（A/B 组） | 地图界面 | 12 | **100%** | 12/12 | **20.8s** |
| ⑪ 抓帧通道 png（A/B 对照组） | 地图界面 | 12 | **100%** | 12/12 | 26.4s |
| ⑫ **面板态起点**（区域进度面板） | 面板态 | 8 | ⚠️ 100%（7/7）**假绿** | ⚠️ **7/8** | 19.8s |
| ⑬ **大世界起点** | 大世界 | 8 | **100%** | **8/8** | 36.5s\* |
| ⑭ **加固后复核**（同参数） | 地图界面 | 12 | **100%** | **12/12** | 29.2s\* |
| ⑮ 记账修复复测 | 地图界面 | 4 | **100%** | **4/4** | 29.0s\* |
| ⑯ **面板态起点·坑⑧ 修复后** | 面板态 | 12 | **100%** | **12/12** | 45.7s\* |
| ⑰ **marker_edit 判据复核**（坑⑨ 修复后） | 地图界面 | 12 | **100%** | **12/12** | 41.8s\* |
| **合计** | | **208** | **100%**（有效轮） | **205/208** | |

> ⑧ 那批的"成功率 100%"是**假的**：12 轮里有 2 轮「目标未在屏 ⇒ 未决」，
> 而"未决"不计入成功率分母 ⇒ 显示 100%。只有「复位 10/12」露了马脚。详见「坑⑤」。
> ⑫ 同理（7/8 在屏）。
> ⑧⑨ 是同一起点、同一代码，只差那一条判据修复；⑫⑬⑭ 是修完坑⑥ 后的三个复核批。
> ⑯ 是**坑⑧ 修复后的真机复核**，起点用的正是当初暴露坑⑧ 的**区域进度面板态**：
> 轮 1 复位路径 `panel/world/map` ⇒ 守卫**正确认出异常起点**并救回（修复前会把 panel
> 误判成 map、直接跳过复位 ⇒ 轮 1 未决）。12/12 全绿、未决 0 ⇒ 修复闭环。
> ⑰ 是**坑⑨（marker_edit 判据）修复后的复核**：12/12 全绿 ⇒ 判据改动无回归。
> 但轮 1 复位路径变成 `panel/unknown/world/map` —— 比 ⑯ **多一步 `unknown`**：
> 判据从"锚点齐 ⇒ map"改成"锚点齐 ⇒ map、**不齐 ⇒ unknown**"后更保守，
> 中间那步落在一个锚点不齐的界面上、不再被 OCR 猜成 map ⇒ 多花一次复位动作。
> **这是取舍不是 bug**：宁可多走一步，也不要假绿。
> \* ⑬⑭⑮⑯⑰ 的单轮耗时偏高**不是代码问题**：那段时间主机负载高（定位从 3.5s 涨到 6.9~14.6s），
> 抓帧 1.8~3.9s。**性能数字不能跨批次比** —— 详见「坑⑥」末尾。

④⑤两轮**逐轮帧差恒为 17.85 / 30.98 / 42.19 / 3.24**（四个目标各 4 次全同）—— 可完全复现。
⑥ 是**故意让每帧都判慢帧并重抓**的加严测试：成功率仍 2/2、帧差与 ⑤ **完全一致**
⇒ 重抓只影响耗时，不影响判据。

### ⑦ 30 轮长跑：无漂移才是重点

| 目标 | 轮数 | 成功率 | 复位 | 逐轮帧差 |
|---|---|---|---|---|
| 眠枭庇护所(地图UI) | 8 | **8/8** | 8/8 | 恒 17.85（首轮 18.55） |
| 皇家招待所 | 8 | **8/8** | 8/8 | 恒 30.98（首轮 31.85） |
| 家园 | 7 | **7/7** | 7/7 | 41.99~42.37（±0.5%） |
| 皮卡月刊 | 7 | **7/7** | 7/7 | **恒 3.24** |

| 汇总 | 结果 |
|---|---|
| 平均单轮 | **21.5 s**（最快 15.9 / 最慢 30.0） |
| 漂移检查 | 前 5 轮均 **21.7s** vs 后 5 轮均 **21.3s**（第 30 轮 24.2s）⇒ 无退化 |
| 抓帧 | 106 次，均 **2017 ms**，最慢 3438 ms，**慢帧 0 轮 / 重抓 0 次** |
| 全库定位 | 30 次，均 **4417 ms**，最慢 6770 ms，命中 **16.1 条/轮** |

三点值得记：

1. **首轮帧差略高是常态**（18.55 / 31.85，之后恒 17.85 / 30.98）：轮 1 是冷启动轮，
   起点守卫与首次定位都在这一轮，画面过渡尚未完全静止。⇒ 定阈值要用第 2 轮起的稳定值。
2. **慢帧阈值 8000ms 在 30 轮里一次都没触发** ⇒ 正常跑批下抓帧兜底是纯保险；
   要验证兜底本身只能像 ⑥ 那样强制触发。
   ⚠️ **但后续批次里它真实触发过三次**（单次抓帧 **39.2s / 64.6s / 64.0s**）——
   三次都在"正常跑批"里、非人工制造 ⇒ **设备/adb 层长卡顿是常态而非异常**，兜底不是过度设计。
   （⑰ 批还出现 `raw` 通道回落 1 次。）被弃的那帧若当结果，帧差会偏到 60+ ⇒ 判据直接歪掉。
3. **长跑的价值在"无漂移"**：逐目标帧差收敛到同一值（皮卡月刊 7 轮全 3.24），
   耗时也不随轮次上升 ⇒ 没有残留状态、帧文件堆积或 OCR 常驻进程泄漏
   （整批只起一次常驻进程）。

## 多图标：从 1 个目标扩到 4 个

单图标跑 40 轮 100% 只证明"**这一个**点得动"。扩目标前必须先逐个实测退出路径
（点进去出不来比点不动更糟）：

| 目标 | 点击反应 | 实测帧差 | 退出方式 | 4 轮结果 |
|---|---|---|---|---|
| 眠枭庇护所(地图UI) | 区域进度面板（风眠省 15/15 + 子区域列表） | 恒 17.88 | ✕ ×1 → panel→world→map | **4/4** |
| 皇家招待所 | 标记编辑态（"标记（点击修改名称）"8/270） | 恒 31.07 | ✕ ×1 → panel→world→map | **4/4** |
| 家园 | 家园信息浮层（等级 25 / 舒适度 / 居住精灵） | 恒 42.19 | ✕ ×1 → panel→map | **4/4** |
| 皮卡月刊 | **只有名字标签**（它不是按钮，是地图标注点） | 恒 2.01 | **再点同位置一次** | **4/4** |

每个目标重复 4 次，**帧差逐轮完全一致** —— 说明"点一次就开面板"在不同目标上都是确定的。

> 顺带发现**复位路径不止一种**：家园点一次 ✕ 就回地图（`panel→map`），
> 而眠枭庇护所(地图UI)、皇家招待所要先落大世界再开地图（`panel→world→map`）。
> ✕ 的语义取决于上层是哪种浮层，不能假设"一次 ✕ 回地图"。

## 单轮 20.8 秒花在哪（提速后口径）

> 下表分项来自第五批 16 轮（均 21.2s，**PNG 通道口径**）。阶段④换 raw 通道后
> 抓帧由 3.7s → 1.9s/帧，单轮随之降到 **20.8s**（同环境 PNG 组为 26.4s），其余分项未动。

| 阶段 | 耗时 | 说明 |
|---|---|---|
| 抓帧 | 7.1s | 3~4 次/轮 × ~2.0s（含复位过程中的抓帧） |
| 全库定位 | 4.2s | 24 模板单尺度；**与 base 的 OCR 并行** |
| 点击与反应验证 | 3.8s | 含"点后等 1.2s 再抓帧"的稳定窗口 |
| 复位流程 | 8.4s | 内含它自己的抓帧与点击（与上面"抓帧"行**有重叠**） |
| **单轮总** | **21.2s** | 30 轮口径 21.5s |

> 各项之和大于总时间：定位与 OCR 并行（时间重叠）、复位段含抓帧（重复计入）。

⇒ 仍然**几乎全是工具侧取证/识别的固定开销**，游戏侧真正的交互（4 次点击注入）
本身不足 1 秒；表里"点击与验证"主要是**为确认结果而留的稳定等待窗口**。
所以"跑一轮 21 秒"不代表游戏卡，而是取证链条的开销。

**提速四阶段（判据全程未动）**：

| 阶段 | 手段 | 单轮 |
|---|---|---|
| 起点 | — | 55.8s |
| ① 去重复劳动 | 复用定位结果判态 / 帧差判态 / 复位复用点击后那帧 | 32.5s |
| ② 挪开 OCR 与定位 | `ocr -serve` 常驻（冷启动整批只付一次）+ base 的 OCR 与定位**并行** | **21.1s** |
| ③ 抓帧兜底 | 超时 60s / 慢帧丢弃重抓 / 新鲜度校验（**纯保险，不改速度**） | 21.1s |
| ④ 换抓帧通道 | raw 取帧 + 本地编码 PNG（绕开**设备端 PNG 压缩**） | **20.8s** |

> ⚠️ 阶段②的 21.1s 与阶段④的 20.8s **不是同一环境**：午间设备侧 PNG 编码从 2.0s 涨到
> 3.7s，**同一份代码**的单轮随之变成 26.4s。所以阶段④的效果要用**同环境 A/B** 来读
> （26.4s → 20.8s，-21%），**不能把 21.1s 与 20.8s 相减说"只快了 0.3s"**。

**阶段④的瓶颈诊断与对照**（这是本批最有价值的一段量化）：

| 通道 | 耗时 | 数据量 |
|---|---|---|
| `shell echo` | **86 ms** | — |
| `exec-out screencap -p`（PNG） | 3468 / 3724 ms | 2.93 MB |
| `exec-out screencap`（raw） | **1166 / 1278 ms** | **9.64 MB** |
| `shell screencap` 落盘 + `pull` | 4515 + 714 ms | — |

⇒ 链路本身很快（echo 86ms）；raw **多传 3 倍数据反而快 3 倍** ⇒
那 2.3s 全花在**设备 CPU 压 PNG** 上，与传输量无关。

| A/B（连续跑，各 12 轮） | raw | PNG | 差异 |
|---|---|---|---|
| 成功率 / 复位 | 12/12 · 12/12 | 12/12 · 12/12 | 一致 |
| **平均抓帧** | **1866 ms** | 3711 ms | **-50%** |
| 平均全库定位 | 3573 ms | 3590 ms | **几乎相同** |
| **平均单轮** | **20.8 s** | 26.4 s | **-21%** |
| 逐目标帧差 | 17.85/30.98/42.13/3.24 | 17.85/30.98/42.2/3.24 | 一致 |

> **定位耗时两组相同**，正是"环境没变、差异来自抓帧通道"的关键对照。
> 换通道前先验等价：**raw 帧 vs PNG 帧逐像素帧差 = 0.0**（同通道噪声基线也是 0.0）。

游戏已复位到干净地图界面，诊断用的一次性帧已清理。

## 八个“给出漂亮假数据 / 假绿”的坑（都已修）

### 坑①：toggle 图标 + 盲点两次 = 帧差 0.00

该图标点一次开面板、再点一次关面板。早期默认**点两次** ⇒ 开了又关 ⇒
逐像素回到原状（帧差 **0.00**），看起来和"点击根本没发出去"一模一样。
判定实验（同一脚本内交替点、每步只点一次）：点地图中部 23.19、点目标 **18.42**、
再点目标**也是 18.42**（与起点同画面）。
⇒ 修法：**点一次 → 抓帧 → 帧差 <3.0 才补点一次**（自适应）。

> 附带更正：排查时我一度把这条假象归因于"沙箱拦截多层 python 调用"，
> 并据此改了 3 处代码 —— **那是错的**，真因只是两次对照实验的点击次数不同。
> 教训：拿"点不动"当结论前先固定点击次数做对照；诊断脚本一次只变一个变量。

### 坑②：自证式判据 —— 复位抓帧覆盖了基准帧

复位"是否已回到地图"用帧差与基准帧比，而复位自己的抓帧**与基准帧共用文件名**
（都不带轮次）⇒ 复位抓帧**覆盖**了基准帧 ⇒ 变成**自己跟自己比 ⇒ 帧差恒 0**
⇒ 永远判"已复位"。更糟：错误状态被下一轮当基准继承，**一路错且不报错**。
⇒ 修法：帧名按轮次隔离 + 同源比较直接返回 `None`（逼调用方走真判据）+ 阈值按语义收紧。

**这个坑只在"异常起点"测试里暴露** —— 正常起点连续跑 10 轮全 100%，
因为起点守卫那段代码**一次都没被执行过**。所以跑批必测两类起点。

### 坑③：自适应补点亲手把成功改写成失败（同一个 toggle 陷阱换了个马甲）

按坑①加了"首点没变化就补点"之后，多图标首版跑批里**皮卡月刊帧差 0.00 判"无反应"**。
它不是点不动，而是它的反应**本来就很小**（它是 toggle 名字标签，点一次只弹一个小标签）：

```
点一次 → 出标签（帧差 2.04）
       → 2.04 < 补点门槛 3.0 ⇒ 判"首点被吞" ⇒ 补点一次
       → 标签被收回 ⇒ 帧差回到 0.00 ⇒ 判"点了没反应"（与"完全没点到"一模一样）
```

⇒ 修法：门槛按**目标类型**给（面板类 8.0 / 标签类 1.5），且标签类**直接关掉补点**
（2.04 vs 1.5 只差 0.5，靠调门槛擦边不稳）。

> 通用教训：**凡"自动重试/自动纠错"的通用保护，作用在边缘目标上时，
> 优先怀疑它是不是在把成功改成失败**，而不是先怀疑链路坏了。

### 坑④：抓帧"兜底"把失败静默变成"拿到旧帧"

16 轮长测里有一轮单轮 64.8s，其中**一次抓帧花了 39.2s**（同轮其他抓帧 2.1s），
而且那帧帧差 **60.64**、与同目标其他轮的 30.98 明显不同 ——
**慢帧通常抓在画面过渡态上，内容不可信**。原实现只把耗时记进统计、不做兜底。

查这个问题的过程中，用离线单测又挖出一个**更隐蔽的真 bug**：抓帧函数在拿不到工具
回传路径时会兜底去读约定文件名 `<tag>.png`，而那是**同名复用**的文件 ——

```
抓帧失败 → 兜底命中"上一轮残留的旧帧" → 当成"本次结果"返回
        ⇒ 后续点击/复位的判据全建在旧画面上，rc=0、不报任何错
```

⇒ 修法三件（判据不变）：子进程超时 60s、单次 >8s 判慢帧**丢弃重抓一次**、
**新鲜度校验**（帧 mtime 必须晚于本次抓帧开始时间）。配套：被丢弃的慢帧挪到
`<tag>.png.slow`（否则下次失败时仍会被兜底捡回来）、被丢弃的耗时单独记账。

> 通用教训：**任何"文件/缓存/上次结果"式兜底，都要能回答"这份数据是不是本次产生的"。**
> 这类 bug 的共同特征：**不报错、指标还好看**，只有**把失败路径单独测一遍**才会露出来。

### 坑⑤：叠加层被误判成底层界面 ⇒ 成功率照样 100%

把起点故意留在**家园信息浮层**（点开家园图标后不回退）跑 12 轮，抓出一个真 bug：

```
轮 1  目标未在屏 ⇒ 未决       ← 起点明明是浮层，却没有任何"起点不在地图界面"的提示
轮 2  皇家招待所 定位 7 条 → 点(177,666) 帧差 17.94 → panel  复位OK(panel/map)
轮 3  目标未在屏 ⇒ 未决
汇总：目标在屏 10/12 · 成功率 100.0%（10/10）· 复位 10/12
```

**根因**：家园浮层是**盖在地图上**的，地图固有词没有消失 ——
判"是不是地图界面"用的两个词**在浮层上同样在屏**：

| 关键词 | 干净地图帧 | 家园浮层帧 |
|---|---|---|
| 卡洛西亚大陆（map 判据） | ✓ ×2 | **✓ ×2** |
| 精灵踪迹（map 判据） | ✓ 11/15 | **✓ 11/14** |
| 舒适度 / 当前居住精灵 / 当前种植植物 | ✗ | **✓** |

⇒ 两个 map 判据词**分不开**这两种界面；而三个浮层特征词在干净地图上**一个都不出现**。
状态判定的顺序里 `marker_edit`、`panel` 都排在 map 之前，**唯独漏了家园浮层**。

**后果是假绿三连**：起点守卫判 map ⇒ 不救；复位探测帧若落在浮层上 ⇒ 也判 map
⇒ **复位假成功**；而目标「家园」图标正被浮层盖住 ⇒ 未在屏 ⇒ 未决。
整套指标里**只有「目标在屏」露马脚，成功率照样 100%**（未决不计入分母）。

⇒ 修法：在 map 判定**之前**插一条叠加层判据（返回新态 `home_panel`），
复位状态机原有的"点一次 ✕"分支自动接管。**离线帧对照 + 真机复跑各 12 轮**验证：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 目标在屏 | 10/12 | **12/12** |
| 未决 | 2 | **0** |
| 复位成功 | 10/12 | **12/12** |
| 轮 1（起点是浮层） | 判 map ⇒ 未决 | **home_panel ⇒ 守卫点 ✕ 救回 ⇒ 成功**（46.2s） |

> 通用教训：**判据顺序必须是"先叠加层、后底层"**。凡是"盖在 A 上的 B"，
> 用 A 的固有特征去认 A，一定会在 B 出现时误判 —— 而且**症状是"一切正常"**。
> 配套心得：**"命中数"不能当界面判据，"锚点齐不齐"可以** ——
> 修复后命中数仍有 11 条 / 20~21 条两档（地图图标动态显隐），但两档都是干净地图、
> **都含两个固定 UI 锚点**；而修复前那个 11 条**没有「家园」**（被浮层盖住）。
> 同是"11 条"，一个是地图、一个是浮层，**只有锚点能分开**。

### 坑⑥：**过渡帧**被判"已回到地图" ⇒ 复位假成功（+ 性能数字不能跨批次比）

修完坑⑤，换个起点类型（**区域进度面板**）再压 8 轮，轮 1 又未决：

```
轮  1  目标未在屏 ⇒ 未决
汇总：目标在屏 7/8 · 成功率 100.0%（7/7）· 复位 7/8
```

明细里守卫**认得对**（`start_state_raw = panel`），复位路径 `['panel','map']` 也"成功"，
但复位后**只命中 7 条、且不含两个固定 UI 锚点**，下一轮起点判出 `world`
⇒ 复位"成功"是假的，实际落到了大世界。

**根因**：那一步抓在**地图正在关闭的过渡帧**上 —— 地图的 UI 文字还在
（含「卡洛西亚大陆」「精灵踪迹」），**图标已经开始消失** ⇒ OCR 判 map。
（单独量大世界画面只有 5 条文字、**不含**「卡洛西亚大陆」⇒ 大世界本身不会被误判。）

**修两件**：

1. **没有 ref 快路径时，"判 map"要再抓一帧确认**（隔 0.6s，过渡态必然继续变化）；
   确认失败就把这步记成 `map_unstable` 继续复位。
   点击后的复位本来就传 `ref=base`（帧差快路径）⇒ 天然免疫过渡态，不必付这次确认。
2. **起点判态改用锚点证据**：把定位**提前到判态之前**，用"两个固定 UI 锚点齐不齐"
   当"是否在地图界面"的像素级判据。定位本来第 2 步就要做 ⇒ **零额外成本**
   （起点在地图就复用那次结果，起点异常则随 base 作废、复位后重定位）。

**验证**：大世界起点 8 轮 **8/8 全绿**（轮 1 被守卫救回，74.9s）+ 加固后同参数 12 轮
**12/12 全绿、复位 12/12、未决 0**。
⇒ **四种异常起点（panel / home_panel / world / 过渡态）现在都收得住。**

> ⚠️ **性能数字不能跨批次比**：这一轮里同一份代码的单轮耗时出现过
> **20.8s / 29.2s / 36.5s** 三个值 —— 变的是**主机环境**（定位 3.5s → 6.9~10.6s，
> 当时主机有 9 个 python 进程在跑、多会话并行），不是代码；抓帧全程稳在 1.8~2.0s。
> **只有"同环境连续 A/B"（如坑④末尾的抓帧通道对照）才说明问题。**

### 坑⑦：汇总里的 `None` —— 记账写错了对象

加固后那 12 轮跑完，汇总里出现 **`平均全库定位: None ms`**。
查下来：`locate(frame, rec, ...)` 的第二个形参**名叫 rec、实际接的是轮记录 `r`**，
而"复用定位"那条新路径里写成了全局 `rec` ⇒ 轮记录里没有 `locate_ms`
⇒ 汇总按轮遍历取不到 ⇒ None。**跑了 12 轮才从汇总的一行 `None` 看出来。**

⇒ 修成 `r.setdefault("locate_ms", [])`；复测 4 轮 `平均全库定位 7824 ms` ✓ 恢复。

> 通用教训：① **新增记账路径前先确认"这个 rec 到底是哪个 rec"**；
> ② **汇总里出现 `None`/`0` 这类"不可能的值"必须当 bug 查**，
> 不能当"没数据"放过 —— 这次它是唯一露出来的线索。

### 坑⑧：**自引入的锚点快路径**把区域面板判成地图 —— 修坑⑥ 时亲手造的新 bug

坑⑥ 的修法里有一条"用两个固定 UI 锚点当像素级判据"（想省掉 3.6s 整帧 OCR），
当时把它放在了**判据最前**：`hits` 命中锚点就直接 `return "map"`。结果 ——

**区域进度面板打开时，地图左侧的「家园」「眠枭庇护所(地图UI)」两个锚点图标仍在屏**
（面板是"盖"在地图上的）。实测面板态 `locate` 命中 **9 条、两个锚点都在其中**
⇒ `MAP_ANCHORS.issubset(hits)` 为真 ⇒ **panel 被判成 map**
⇒ 起点守卫认成"已经在地图、无需复位"、复位末步也"成功" ⇒ 又一种假绿，
而且这次是**自己刚加进去的**。

**关键认知**：锚点是**必要条件，不是充分条件**。
"锚点齐 ⇒ 在地图"**不成立**，只能反过来说"**锚点不齐 ⇒ 一定不在干净地图**"。
（坑⑤ 那条结论"命中数不能当界面判据、锚点齐不齐可以"要补一句：
锚点齐**只能当地图的下界**，不能单独当判据。）

**修**：`detect_state` 判据顺序改为

```
ref 帧差快路径 → 叠加层(world / marker_edit / panel / home_panel)
→ 有 hits 则以锚点为准（齐 ⇒ map，不齐 ⇒ unknown）→ 才落 OCR 地图词兜底
```

锚点分支的"不齐"直接给 `unknown`（有 hits 说明定位已经跑过，不该再靠 OCR 猜）。

**验证**：离线帧 + 真机各一遍（真机那遍就是第 ⑯ 批，见上表：面板态起点 12/12、未决 0）。

> 教训：**为消灭一个误判而新增的快路径，本身就是新的误判面。**
> 快路径"零成本"只说明它**省了计算**，不代表它**判得对** ——
> 凡是"某特征命中就断定界面"的写法，都要先问一句"**这个特征在别的界面上会不会也在屏**"。

### 坑⑨：长词判据被 **OCR 错字**打穿（标记编辑态漏判）

建离线回归集后**第一次跑就抓到**：`s_now5_panel.png` 是**标记编辑态**，
但 OCR 把「点击修改名称」认成了「**点击修破名称**」⇒ 长词判据 `"标记（点击修改名称）"`
不匹配 ⇒ 判成 `unknown`。
（危害比假绿小：`unknown` 会触发守卫复位，**只是多花一次复位**，不会假装成功。）

**修**：补一个标记界面独有词当互补判据 —— `"常规标记数" in joined`；回归集 5/5 全绿。

## 离线判据回归集（`tools/nrc_state_regress.py`）

坑⑤⑥⑧ 全都是**判据顺序**问题，而每次都要**真机造异常起点**才能暴露（贵、难复现）。
⇒ 把各类界面各存几帧当回归集，改 `detect_state` 前后各跑一遍，**纯离线、不碰真机**。

当前用例集（帧已归档到 `.workbuddy/evidence/nrc/regress-frames/`，属本地资产、未入 git）：

| 场景 | 帧（归档名） | 原始名 | 期望 | 命中 | 锚点 | 结果 |
|---|---|---|---|---|---|---|
| 区域进度面板（坑⑧ 现场） | `panel.png` | `regress_post.png` | panel | 9 | 齐 | ✓ |
| 干净地图 | `map_clean.png` | `regress_pre.png` | map | 11 | 齐 | ✓ |
| 家园界面（坑⑤ 现场） | `home_panel.png` | `g4_panel.png` | home_panel | 4 | 不齐 | ✓ |
| 大世界（坑⑥ 现场） | `world.png` | `dw_world.png` | world | 0 | 不齐 | ✓ |
| 标记编辑态（坑⑨ 现场） | `marker_edit.png` | `s_now5_panel.png` | marker_edit | 0 | 不齐 | ✓ |

> 帧原先放在 `tmp/screenshots/` —— 而 **tmp 随时可能被清理**，回归集就废了 ⇒
> 已归档进证据目录，并统一改成语义名（原始名 `g4_panel` 带 panel、实际是家园态，
> 正是它会骗人）。

**首次运行就出了三件事**，很能说明这类回归集的价值：

1. **两处期望值是错的 —— 我按帧名猜的**：`g4_panel` 名字里有 panel，实际是**家园界面**；
   `regress_pre` 被当成面板，实际是**干净地图**。
   ⇒ **帧名/命名不能当判据**，期望值必须"人看帧 + 看 OCR 全文"得出。
2. **一处真漏判**（坑⑨）—— 真机连跑多少轮都不会遇到（要 OCR 恰好认错字）。
3. **验证脚本自己也会骗人**：第一版离线验证把 `locate` 返回的 `(hits, score)` **元组
   整个当 hits 传**了进去，而 panel 判据排在锚点分支**之前**就 `return` 了
   ⇒ **根本没走到被测分支**，却打印出一个"通过"。
   ⇒ 验证要能看到"**真的走进了那条分支**"（本工具因此把 A/B 两条口径与命中数都打出来）。

> 还缺一帧：**地图关闭过渡帧**（坑⑥ 现场）没有存档，当时是实时抓的 ⇒ 待补。

## 产物

- 工具：`tools/nrc_soak.py`（跑批，`--targets` 选目标、`--shot-channel raw|png` 选抓帧通道）、
  `tools/icon_match.py`（定位+点击，`--tap-verify`）、
  `tools/nrc_state_regress.py`（**离线判据回归集**，改 `detect_state` 前后各跑一遍）
- 图标库：24 条模板（`.workbuddy/nrc/icon_atlas/library/`，本地资产，未入库 git）
- 证据：`.workbuddy/evidence/nrc/` 下
  `20260921-soak/`（单图标 40 轮）、`20260921-soak-multi/`（多图标 16 轮）、
  `20260921-soak-30/`（30 轮长跑）、`20260921-soak-abnormal{,2}/`（异常起点 修复前/后）、
  `20260921-chan-{raw,png}/`（抓帧通道 A/B）、`20260921-soak-start-{panel,world}/`（面板态/大世界起点）、
  `20260921-soak-hardened/`（加固后复核）、`20260921-soak-fixcheck/`（记账修复复测）、
  `20260921-soak-anchor/`（**面板态起点·坑⑧ 修复后** 12 轮）、
  `20260921-soak-mkfix/`（**marker_edit 判据复核** 12 轮）、
  `regress-frames/`（**离线判据回归集**的 5 帧，语义名）
  —— 每目录都有 `soak.json` + 带时间戳存档
- 方法论文档：`docs/nrc-icon-atlas-method.md` §5.8（性能四阶段 / 各基线 / 叠加层判据 / 过渡帧判据）、
  §5.9（多图标）；`docs/nrc-field-notes.md` §11.30 / §11.31 / §11.32 / §11.33

## 下一步（按性价比）

1. **异常起点这条线已经跑通四种**（panel / home_panel / world / 过渡态），
   每次都出货（坑⑤⑥ 都是它抓出来的）⇒ 后续要再压，优先**造新的异常起点**
   （别的浮层、别的游戏内区域、切图后立刻抓帧），而不是加正常轮数。
2. **性能数字要"同批同环境"读**：主机负载会让定位在 3.5s~10.6s 之间漂，
   单轮 20.8s~36.5s 都可能出现。**跨批次比较无效**；
   要报提速就做同批 A/B（本报告的抓帧通道对照就是这么做的）。
3. **判据侧是下一个提速点**：定位固定 3.5s+（未动过）。缩小 ROI / 降采样模板匹配
   比继续抠抓帧更划算（抓帧已到 1.9s）。
4. ~~给状态判据补"回归用例集"~~ ✅ **已落地**：`tools/nrc_state_regress.py`，
   已收 5 帧 5 态（干净地图 / 家园 / 区域面板 / 大世界 / 标记编辑）**全绿**；
   首次运行就抓出 2 处标错的期望值 + 1 处真漏判（坑⑨）。
   **还缺"地图关闭过渡帧"一帧**（坑⑥ 现场，当时是实时抓的、没存档）⇒ 补上才算齐。
   **以后改 `detect_state` 必须先跑这个回归集，再上真机。**
5. **把"反应类型 + 退出路径"写进图标库**：退出策略仍硬编码在 `TARGETS` 表里，
   库里还没有 `reaction`（panel/label/marker/dead）与 `exit`（close/retap）字段。
6. 图标库 8 条"单帧出现" + 2 条"未出现"待换帧补验。
