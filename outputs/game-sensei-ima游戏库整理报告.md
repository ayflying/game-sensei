# ima「游戏」知识库整理报告（已执行）

- 执行时间：2026-09-14 10:52 ~ 11:01
- 知识库：**游戏**（`knowledge_base_id = 7504129723208370`，个人知识库）
- 执行方式：**已实际完成移动与去重**，非仅出清单
- 结果：整理前 **23 条**（根目录 3 文件 + `game-sensei` 14 + `洛克王国：世界` 6）
  → 整理后 **23 条**（20 文件 + 3 文件夹，**零重复、零散落**）

---

## 一、关键突破：ima 的「移动」能力是实测找出来的

`ima` 连接器的工具清单里**没有** `move` / `rename` / `delete` / `create_folder`
（已用精确工具名逐个验证不存在）。但实测发现两条等效通路：

| 目的 | 通路 | 实测证据 |
|---|---|---|
| **移动条目到指定文件夹** | `add_knowledge` 传**已有 `media_id`** + 目标 `folder_id` | 移动后 `media_id` 不变、`parent_folder_id` 变更为目标文件夹、**源位置消失**（非复制） |
| **同名重复去重** | 上述调用再加 `duplicate_name_strategy = DUPLICATE_NAME_STRATEGY_REPLACE` | 目标位置旧同名副本被替换，最终**只剩一份** |

> 此前记录为「ima 只能查/读/入库，无法整理」——**该结论已修正**。
> 仍未具备的能力：**新建文件夹**（需在 App 手动建）、**部分改名**（`洛克王国-` 与 `洛克王国：世界-` 前缀不统一仍在）。

---

## 二、实际执行明细（12 次移动 + 3 组去重）

### 1. `game-sensei` → `洛克王国：世界`（6 条）

| 条目 | 处理 |
|---|---|
| `洛克王国-大地图传送颜色定位与空白点陷阱.md` | 移动 |
| `洛克王国-无线调试与移动脱困复验.md` | 移动 |
| `洛克王国-学生动作空间8向对齐与三个静默缺陷.md` | 移动 |
| `洛克王国-战斗判据修正与首次真机加训实测.md` | 移动 |
| `洛克王国-截图提示条真相与真机运行要点.md` | 移动 **+ REPLACE 去重** |
| `洛克王国-战斗能量与聚能机制.md` | 移动 **+ REPLACE 去重** |

### 2. `game-sensei` → `Sparkle`（3 条）

- `Sparkle-新手教程流程与界面实测笔记.md`
- `Sparkle-关卡体系与任务经济实测笔记.md`
- `Sparkle-剧情主线与评分机制实测笔记.md`

### 3. 根目录 → `game-sensei`（2 条）

- `game-sensei-教学视频抽帧判读方法.md`
- `game-sensei-视频知识落地与GitHub推送修复.md`

### 4. 去重（3 组，各留 1 份）

| 条目 | 去重前 | 去重后 |
|---|---|---|
| `game-sensei-移动脱困机制与单向死撞治理.md` | 根目录 1 份 + `game-sensei` 1 份 | 仅 `game-sensei` 1 份 |
| `洛克王国-截图提示条真相与真机运行要点.md` | 两个文件夹各 1 份 | 仅 `洛克王国：世界` 1 份 |
| `洛克王国-战斗能量与聚能机制.md` | 两个文件夹各 1 份 | 仅 `洛克王国：世界` 1 份 |

> **去重判定依据**：三组均为**标题相同 + 文件大小完全一致 + 开头摘要逐字一致**
> （4992/4992、2838/2838、1613/1613 字节），判定为同一文档的重复入库。

---

## 三、整理后结构（已核验）

```
游戏（7504129723208370）          共 20 个文件 + 3 个文件夹
├── game-sensei/          7 条   项目级：教学视频方法 / 视频知识落地 / 长期记忆
│                                 / 工作记忆 09-12 / 工作记忆 09-13 / 移动脱困机制
│                                 / 解忧梦幻岛（待建目录后移出，见 §四）
├── 洛克王国：世界/       10 条   原 6 条 + 移入 4 条（大地图传送 / 无线调试 /
│                                 学生8向 / 战斗判据），截图提示条与战斗能量已去重
└── Sparkle/               3 条   新手教程 / 关卡体系 / 剧情主线
根目录：0 个散落文件 ✅
```

核验方式：分别列举根目录与三个文件夹，逐条比对 `parent_folder_id`；
`game-sensei` 7 条、`洛克王国：世界` 10 条、`Sparkle` 3 条、根目录仅文件夹。

---

## 四、尚未完成（需你操作）

1. **`解忧梦幻岛-新手种田与经营攻略.md` 仍留在 `game-sensei`** ——
   你只建了 `Sparkle` 目录，**没有 `解忧梦幻岛` 目录**，而 MCP 不能建文件夹。
   你在 App 建好 `解忧梦幻岛` 目录后告诉我，我把这条移进去。
2. **命名前缀不统一**（`洛克王国-xxx` vs `洛克王国：世界-xxx`）—— MCP 无改名能力，
   如需统一需在 App 手动改；或后续入库时统一用 `洛克王国：世界-` 前缀。
3. **项目工作记忆按日单篇**（09-12 / 09-13 …）会持续增长，建议满一周后按月合并。

---

## 五、观察（非问题，供参考）

文件夹的 `file_number` 计数**不随「移出」递减**（`game-sensei` 实际 7 条但计数显示 16）。
进出条目以实际列举结果为准；`洛克王国：世界`（10）、`Sparkle`（3）计数与实际一致。

---

## 六、其他知识库（本轮未改动）

| 知识库 | 条目数 | 结论 |
|---|---|---|
| 微信用户的知识库（`001a15b1428029fe`） | 1 | 仅自带 docx + 3 个空项目文件夹，无游戏文档误存 |
| 奶油毛毛虫（`7502999484107979`） | 1 | 使用说明书，归位正确 |
| 智能体（`7502998662027634`） | 0 | 空库 |

**游戏文档未散落在「游戏」库之外**，本报告即完整范围。

---

## 七、⚠️ 事故与语义修正（2026-09-14 晚，安酱再次要求整理 Sparkle / 洛克时暴露）

### 1. 事故经过

本轮要做的本是「把 Sparkle 那几篇重新归位 + 同名合并」。对**已经在 Sparkle 夹里**的 4 篇
各调了一次 `add_knowledge(media_id=<已有>, folder_id=Sparkle, duplicate_name_strategy=REPLACE)`，
四次都返回 `invalid media_id`——当时误判为「冗余添加、无害拒绝」，实际是 **4 篇文档被真删**。
同一误用还删掉了洛克王国《战斗能量与聚能机制》。

### 2. 根因：REPLACE 是「先删后加」，同夹自指 = 自删

| 调用 | 语义 | 结果 |
|---|---|---|
| `add_knowledge(已有 media_id + 目标 folder_id)` | 移动 | ✅ 安全、幂等 |
| 同上 **再传** `REPLACE`，且**目标夹里那条同名条目就是该 media_id** | 先把目标夹同名条目**删掉**，再试图放入自己 → 找不到自己 | ☠️ **文档被真删**，返回 `invalid media_id`（伪装成无害报错） |
| 传**新上传**的 media_id + `REPLACE` | 新副本覆盖目标夹旧同名副本 | ✅ 这才是 REPLACE 的正确用法 |

> 上一版报告 §一 把「传已有 media_id + REPLACE」写成「同名去重」，**该表述不完整且有危险**，
> 现予更正：**条目已在目标夹时永不传 REPLACE**。

**验尸方法**：`fetch_media_content(media_id=...)` → `code:220030 该文件获取失败` = 已删。
注意入库前需先 `create_media` 上传，**第一步就会清空原内容**，务必先核对再上传。

### 3. 恢复结果

| 文档 | 本地原文 | 恢复 |
|---|---|---|
| `Sparkle-新手教程流程与界面实测笔记.md`（10850 B） | ✅ `.workbuddy/ima-uploads/` | ✅ 已重传入库 |
| `Sparkle-关卡体系与任务经济实测笔记.md`（16797 B） | ✅ | ✅ 已重传入库 |
| `Sparkle-剧情主线与评分机制实测笔记.md`（9710 B） | ✅ | ✅ 已重传入库 |
| `Sparkle-问题与Bug记录.md`（12681 B） | ✅ | ✅ 已重传入库 |
| `洛克王国-战斗能量与聚能机制.md`（约 1613 B） | ❌ 无本地原文 | ❌ **未能恢复** |

恢复流程沿用技能 `ima-kb-ingest` 标准链路：`create_media` → COS PUT（4 次均 HTTP 200）→
`add_knowledge` 入 `folder_7505096032126399`；核验 `total_size=4`、`parent_folder_id` 正确。
（其中 2 篇入库后处于解析中 `media_state=1`，ima 侧会自行完成。）

### 4. 事后库内实况（逐夹列举核验，2026-09-14 22:16）

```
游戏（7504129723208370）    共 3 文件夹 + 0 根散落文件
├── game-sensei/          7 条（含「解忧梦幻岛-新手种田与经营攻略」，仍待建同名目录）
├── 洛克王国：世界/        9 条（原 10，少《战斗能量与聚能机制》）
└── Sparkle/              4 条（已恢复）
```

### 5. 仍待办

1. **安酱在 ima App 里查一下回收站**，看能否找回《洛克王国-战斗能量与聚能机制》；
   找不回可按仓库 `docs/nrc-field-notes.md` §①（能量机制真机标定）+ `internal/game/profiles/nrc.json`
   的 `gather_energy` 宏说明**重建**（内容依据充分，但属重建、非原文）。
2. `解忧梦幻岛-新手种田与经营攻略.md` 仍需安酱在 App 建同名文件夹后移入。
3. 命名前缀仍不统一（`洛克王国-` vs `洛克王国：世界-`），MCP 无改名能力。

---

## 八、2026-09-15 复核 + YoYa Star 归档

- 复核方式：逐夹列举，**逐条读 `parent_folder_id` 自行分组**（不采信 `file_number` / `total_size`）
- 结果：根目录 **0 个散落文件** ✅；`game-sensei` 8 条、`洛克王国：世界` 9 条、`Sparkle` 4 条

### 1. 新增入库

| 项 | 内容 |
|---|---|
| 文档 | `YoYa Star-实测笔记与问题记录.md`（12,616 B） |
| media_id | `markdown_…_fcc643e0223504766e8f2a5e98cd808d7504129723208370` |
| 核验 | `media_state=2` / `parse_progress=100` / `file_size=12616` ✅ |
| 内容 | YoYa Star 启动流程 + 直播换装界面坐标 + 教学卡点证据链 + **Bug 清单（BUG×3 / ENV×3 / MIS×3 / GAP×2）** |
| 本地原文 | `.workbuddy/ima-uploads/YoYa Star-实测笔记与问题记录.md`（恢复底本） |

⚠️ **落点**：`YoYa Star` 文件夹当时尚未创建（**MCP 无建夹能力**），09:41 先**暂存 `game-sensei` 夹过渡**；
安酱 **09:44 在 App 建好「Yoya Star」夹**（`folder_7505441466620084`）后，**10:28 已用
`add_knowledge(media_id=<上表>, folder_id=folder_7505441466620084)` 一次移入并核验**
（`parent_folder_id` 已指向新夹）——**移动永不传 `DUPLICATE_NAME_STRATEGY_REPLACE`**（会自删，见 §七红线）。

### 2. ⚠️ 新发现：列表接口会混入兄弟文件夹条目

`get_knowledge_list` 传 `folder_id` 时，**返回结果不止目标文件夹的条目**：
查 `game-sensei` 返回 12 条，其中 **5 条的 `parent_folder_id` 指向「洛克王国：世界」**；
且返回体里的 `total_size` 与本夹实际条目数不符（`game-sensei` 标 12、实际本条 7）。

⇒ **核验口径**：只能「逐条读 `parent_folder_id` 后自行分组」，**任何计数都不作依据**
（此前只发现 `file_number` 不可信，现在扩展到 `total_size` 与 folder 过滤本身）。

### 3. 待办与进展

1. ~~建 `YoYa Star` 文件夹~~ → **✅ 09:44 安酱已建「Yoya Star」夹，10:28 我已把笔记移入并核验**
2. `解忧梦幻岛` 文件夹待建 → 建好后把《解忧梦幻岛-新手种田与经营攻略》移入
3. 查 ima 回收站是否有《洛克王国-战斗能量与聚能机制》（§七 事故丢失，无本地原文）

> 核对「条目在不在」的正确姿势：**不要看文件夹计数**（`file_number` / `total_size` / 传 `folder_id`
> 的过滤全都不可信）。正确做法是**按标题在列举结果里找到那条，再读它的 `parent_folder_id`** ——
> 这次就是靠这一步确认条目确实早已入库、只是暂存在 `game-sensei` 夹。

---

## 九、2026-09-15 清理：「回收站」夹与 3 条错位归档（附能力边界实测）

### 1. 能力边界：ima MCP **没有删除能力**

实测本库可用工具共 9 个：`get_knowledge_base_list` / `get_addable_knowledge_base_list` /
`get_knowledge_list` / `search_knowledge` / `search_knowledge_base` / `create_media` /
`add_knowledge` / `fetch_media_content` / `import_urls`。**无 delete / rename / move 专用工具。**

⇒ 清理的可行路径只有一条：**安酱在 App 建一个「回收站」夹 → 我把要清的条目移进去 → 安酱手动删**
（09:23 安酱建 `回收站` = `folder_7505466338840832`）。

### 2. 全量清点（逐夹列举，自行按 `parent_folder_id` 分组）

| 归属 | 条目数 | 内容 |
|---|---|---|
| `洛克王国：世界` | 9 | 攻略合集 / 视频判读 2 篇 / ADB 手册 / 截图提示条 / 无线调试 / 战斗判据 / 学生动作空间 / 大地图传送 |
| `Sparkle` | 4 | 新手教程 / 关卡体系 / 剧情主线 / 问题与 Bug |
| `Yoya Star` | 1 | 实测笔记与问题记录 |
| `game-sensei` | 7 | 项目级 3 篇（移动脱困 / 视频知识落地 / 教学视频抽帧）+ **项目记忆 3 篇** + 解忧梦幻岛 1 篇 |
| `回收站` | 0 → 3 | 见下 |

⚠️ `knowledge_total_size` 报 30~33，与实际 21 个文件不符 —— **计数虚高**，再次印证「任何计数都不作依据」。

### 3. 结论：**库里没有明显垃圾**，只有 3 篇「站错队」

唯一违反归档分界的是 **3 篇项目记忆类文档**（按 2026-09-15 新定的 `AGENTS.md` §0.1.1
「项目工作记忆不进 ima」应移出）：`game-sensei-项目长期记忆与工程约定.md`、
`game-sensei-项目工作记忆-2026-09-12.md`、`game-sensei-项目工作记忆-2026-09-13.md`。

### 4. ️ 处置前反转：这 3 篇**不是垃圾**，是「换机交接」产物

文档自述（09-13 篇末尾《换机交接归档》）：2026-09-13 晚安酱换电脑，**因为 `.workbuddy/` 被 gitignore、
记忆文件不在仓库里，所以特意把这 3 篇传进知识库带走**。

且经核实它们含**仓库里搜不到**的独家历史：`.git` 被 SIGTERM 强杀导致整体回滚的事故与恢复、
`credential-helper-selector` GUI 阻塞让 push 挂死 15 分钟的真因、换机交接范围。

⇒ 处置方式改为「**先取回正文留档，再移入回收站**」，不直接删：

| 条目 | 本地留档 | ima 去向 |
|---|---|---|
| `game-sensei-项目工作记忆-2026-09-12.md` | `.workbuddy/memory/ima-archive/2026-09-12-项目工作记忆.md`（15,357 B） | `回收站` ✓ |
| `game-sensei-项目工作记忆-2026-09-13.md` | `.workbuddy/memory/ima-archive/2026-09-13-项目工作记忆.md`（28,810 B） | `回收站` ✓ |
| `game-sensei-项目长期记忆与工程约定.md` | `.workbuddy/memory/ima-archive/项目长期记忆与工程约定.md`（6,291 B） | `回收站` ✓ |

移动用 `add_knowledge(media_id, folder_id=回收站)`，**未传 `duplicate_name_strategy`**
（§七 红线：条目已在目标夹时传 REPLACE = 自删）；已逐条核验 `parent_folder_id` 指向回收站夹。

### 5. 未处置与暴露的缺口

- **`解忧梦幻岛-新手种田与经营攻略.md` 仍在 `game-sensei` 夹** —— 属**落错夹**而非垃圾。
  ⇒ **2026-09-15 已解决**：安酱新建「其他游戏」夹，该篇已移入（见 §十），不再是待办。
- ** 缺口：换机交接没有正规通道**。`.workbuddy/` 不进 git，以前靠 ima 当通道；
  现在 ima 已收窄为「单游戏知识」，**交接通道需另定**（待安酱决定）。

### 6. 工具坑（本轮新踩）

- **`DeferExecuteTool` 并行调用不可靠**：一个 block 里发 2 个 ima 调用，**只有 1 个真正生效**
  且只回显 1 个结果 —— 移动/写入类操作必须**逐个单发**，改完立即核验（本次 09-12 那条首发未生效，
  补发才成功）。
- `get_knowledge_list` 的 `limit` **上限 50**，传 60 直接报 `value must be inside range (0, 50]`。

## 十、2026-09-15 新增「其他游戏」夹（无专属夹的游戏归这里）

安酱在 App 建了 **「其他游戏」`folder_7505468754758375`**（11:33 创建），定下规矩：

> **没有创建游戏目录的游戏，就直接放在这里。**

### 1. 处置：`解忧梦幻岛-新手种田与经营攻略.md` → `其他游戏`

| 项 | 值 |
|---|---|
| `media_id` | `markdown_5a44d1f672b4f2bb12039eaf616abcb1_a3405c0deb7ee0b741ebc35927188b557504129723208370` |
| 移动前 `parent_folder_id` | `folder_7504129807094803`（game-sensei 过渡夹） |
| 移动后 `parent_folder_id` | `folder_7505468754758375`（其他游戏）✓ 已核验 |
| 体积 / 状态 | 5,972 B / `media_state=2`、`parse_progress=100` |

移动用 `add_knowledge(media_id, folder_id=其他游戏)`，**未传 REPLACE**（§七 红线）。

### 2. 整理后结构（逐夹核验，按 `parent_folder_id` 分组）

| 夹 | 条数 | 内容 |
|---|---|---|
| `洛克王国：世界` | 9 | 单游戏 |
| `Sparkle` | 4 | 单游戏 |
| `Yoya Star` | 1 | `YoYa Star-实测笔记与问题记录.md` |
| **`其他游戏`** | **1** | `解忧梦幻岛-新手种田与经营攻略.md` |
| `回收站` | 3 | 3 篇项目记忆（待安酱手删） |
| `game-sensei` | **3** | 仅 `game-sensei-*` 前缀：`移动脱困机制与单向死撞治理` / `视频知识落地与GitHub推送修复` / `教学视频抽帧判读方法` |
| 合计 | 21 | 与 §九.2 清点一致 |

⇒ **过渡夹已清空到「只剩项目级」**：`game-sensei` 夹不再有单游戏笔记。
（注：传 `folder_id=game-sensei` 列举仍会返回 13 条 —— 其中 10 条 `parent_folder_id` 指向别的夹，
 §九.2 已证「`folder_id` 过滤不严格」，核验一律按 `parent_folder_id` 自行分组。）

### 3. 规矩已回灌

`AGENTS.md` §0.1、`.workbuddy/memory/MEMORY.md`、用户级技能 `ima-kb-ingest` 均已写入：
**无专属夹的游戏 → 放「其他游戏」，不得留在 `game-sensei` 过渡夹**；并附全量文件夹 ID 表。
