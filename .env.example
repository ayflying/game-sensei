# 复制为项目根目录 .env 并填入真实配置；.env 不得提交。
# 渠道配置仅用于图片识别。地址支持 /v1 基址或完整 /chat/completions。
AIFERRY_BASE_URL=
AIFERRY_API_KEY=
# 按优先级排列的视觉模型，逗号分隔；不提供隐藏的默认模型。
AIFERRY_MODELS=

# 本地兜底：填写完整地址，或只填写本地 OLLAMA_PORT。
OLLAMA_BASE_URL=
OLLAMA_PORT=
OLLAMA_MODEL=

# 每次请求的超时时间。
VLM_TIMEOUT=120s
# 可选：渠道直连网络失败后的代理；本地 Ollama 不经过代理。
VLM_PROXY_URL=
